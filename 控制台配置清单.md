# 控制台配置清单 — 请按顺序操作

> 环境：`see-boss-d2gjggfbz8808d1d4`（上海 / ap-shanghai）
> Publishable Key 已收到并写入 `.env`（原 `Publishable_KEY.txt` 已删除）

---

## ① 开启匿名登录 ⚠️ 必做

**路径**：控制台左侧「身份认证」→「登录方式」→ 找到「**匿名登录**」→ 开启

**为什么必做**：
只用 Publishable Key 而不做 `signInAnonymously()`，所有玩家会共享同一个 `sub="anon"`，RLS 的 `auth.uid()` 无法区分用户 → **所有人的金瓜子、底牌会互相可见**。

---

## ② 执行 schema.sql ⚠️ 必做

**路径**：控制台左侧「SQL 型数据库」→「PostgreSQL 管理」→「**数据编辑器**」

**操作**：打开 [db/schema.sql](../db/schema.sql)，**按注释分段执行**（0 ~ 17 段）。

⚠️ 两个坑：
- `ExecutePGSql` **每次只能执行一条 SQL**，不能用分号拼接多条
- 部分 DDL 直接执行可能报 `InternalError`，用 `DO $$ ... $$` 块包装重试

**建议执行顺序**（每段单独粘）：
| 段 | 内容 |
|---|---|
| 0 | 清理（DROP，重跑时才需要） |
| 1-7 | 枚举 + 6 张表 + history |
| 8 | 索引 |
| 9 | `user_seed_stats` 视图 |
| 10 | `touch_updated_at` 函数 + 触发器 |
| 11 | 表级 GRANT |
| 12 | RLS 策略 |
| 13-16 | 4 个 RPC 函数 |
| 17 | CDC publication。**第 17 段若报 `role "cloudbase_realtime_admin" does not exist` 是预期的** —— 已改成条件执行跳过授权，publication 仍会建成功。想确认 CDC 是否真可用，跑 [db/diagnose-cdc.sql](../db/diagnose-cdc.sql) |

**验证**（在数据编辑器执行）：
```sql
select tablename from pg_tables where schemaname='public' order by tablename;
```
应返回 7 张：`decks` `hands` `history` `room_members` `rooms` `seed_ledger` `users`

```sql
select tablename, policyname from pg_policies where schemaname='public';
```
应返回所有 RLS 策略。

---

## ③ 配安全来源白名单 ⚠️ 必做

**路径**：控制台左侧「设置」→「**安全来源**」（你截图里的「安全管控」页面）

**添加**：
```
localhost:5173
127.0.0.1:5173
```

**为什么**：浏览器直连 PG 走 PostgREST HTTP 网关，不加白名单会 CORS 报错。约 1-2 分钟生效。

> 上线后还要加生产域名，如 `xxx.tcloudbaseapp.com`

---

## ④ 跑自检

三件套配好后：

```bash
# D:\newPoker\
# 编辑 .env，把 VITE_DATA_SOURCE 改成 cloud
notepad .env
```

```bash
npm run dev
```

浏览器打开 `http://localhost:5173`，按 **F12** 打开 Console，执行：

```js
await window.__cbCheck()
```

会依次检查 7 项并给出 ✓/✗：
1. 环境变量
2. 匿名登录
3. 7 张表是否存在
4. RLS 隔离（读不到别人的数据）
5. 写入 users 权限
6. RPC `transfer_seeds`
7. 实时订阅

**把 Console 的输出截图发我**，我根据失败项定位。

---

## 之后还要做的（依赖自检结果）

| 项 | 说明 |
|---|---|
| **云函数能否连 PG** | 免费版云函数不支持 VPC。若不连通，逻辑前移到 PG 的 `SECURITY DEFINER` 函数 |
| **实时策略选型** | 默认 `poll`（轮询）最稳；`broadcast` 不消耗资源点但需服务端配合；`cdc` 官方文档自相矛盾需实测 |
| **前端切到 repo** | 现在业务代码还在用本地状态，等自检通过后统一改 |

---

## 当前状态

- ✅ Publishable Key 已写入 `.env`，`.gitignore` 已排除
- ✅ `VITE_DATA_SOURCE` 暂时保持 `local`，等你配好再改 `cloud`（避免现在打开就报错）
- ✅ 数据层代码、schema.sql、自检脚本全部就绪
- ⏳ 等你完成 ①②③
