# 零成本最终方案：3-5 好友 H5 德扑

> 2026-09-20 定稿 ｜ 目标：**0 元**，不买域名，顺畅实时通信、算分、存分
> 取代前几篇的部署讨论：[部署方案对比.md](部署方案对比.md) ｜ [零费用部署方案.md](零费用部署方案.md)

---

## 0. 结论

**买域名是纯浪费，轻量服务器也是纯浪费。CloudBase 免费体验版 + 静态托管 + 云数据库，0 元完全够用。**

理由：
- 域名只有在你想让好友记住一个好看地址时才需要。微信群发一个链接，用默认域名就够
- 3-5 人的实时同步，`watch()` 一条订阅搞定，不需要 WebSocket 服务器
- 你要求的"存储分数和个人信息"，文档型数据库免费额度内绰绰有余

---

## 1. 最终架构

```
好友的手机浏览器
      │  https://xxxx.tcloudbaseapp.com
      ▼
┌─────────────────────────────────────────────┐
│  CloudBase 静态网站托管（Vue3 + Vite 产物）    │
│  默认域名自带 HTTPS + CDN，无需配置            │
└──────────────┬──────────────────────────────┘
               │ @cloudbase/js-sdk（匿名登录）
               │
     ┌─────────┴──────────┬──────────────┐
     ▼                    ▼              ▼
┌──────────┐      ┌────────────┐   ┌──────────┐
│ 云函数   │      │ 文档型数据库 │   │ 云函数   │
│ 游戏逻辑 │◄────►│  rooms      │   │ 结算     │
│ (权威)   │      │  hands      │   │          │
└──────────┘      │  users      │   └──────────┘
                  └──────┬───────┘
                         │ watch() 实时推送
                         ▼
                  所有玩家的手机同时刷新
```

**关键设计：游戏逻辑放云函数，不放房主手机。**

这一点很重要。如果让房主手机当"服务器"，会有三个真实问题：
1. **作弊**：房主要负责洗牌发牌，他的浏览器知道整副牌
2. **手机锁屏**：浏览器会节流后台定时器，下注倒计时会卡住
3. **房主关页面**：对局状态直接悬空

放进云函数后：逻辑在服务端执行，客户端只负责渲染。免费版云函数超时 3s——做几次数据库操作绰绰有余。

### 三个数据集合

| 集合 | 内容 | 安全规则 |
|---|---|---|
| `users` | 昵称、头像、金瓜子、总胜场 | `read/write: doc._id == auth.uid` |
| `rooms` | 房间公共状态（成员、筹码、公共牌、奖池、当前轮次） | `read: auth.uid in doc.members`<br>`write: false`（只准云函数写） |
| `hands` | 每个玩家的底牌 | `read: doc.uid == auth.uid`<br>`write: false`（只准云函数写） |

**为什么 `hands` 要单独放**：如果把整副牌塞进 `rooms`，所有人 watch 一次就全看到了。单独存 + 安全规则限本人读，才能保证只有本人看得见自己的底牌。

**为什么 `rooms` 的 `write` 设为 false**：前端不能直接改房间状态，所有变更必须经过云函数校验。否则谁都能改自己筹码。

---

## 2. 费用实测估算（远低于免费额度）

| 项目 | 单价 | 月用量估算 | 月耗资源点 |
|---|---|---|---|
| 静态托管流量 | 210 点/GB | 5 人 × 5 次 × 1MB ≈ 25MB | ~5 |
| 数据库调用 | 200 点/万次 | 100 局 × 150 次读写 = 15,000 | ~300 |
| 云函数调用 | 13.3 点/万次 | 15,000 次 | ~20 |
| 云函数资源 | 0.11108 点/GBs | 15,000 × 0.2GBs | ~333 |
| 身份认证 | 500 点/万次 | 20 次 | ~0.1 |
| **合计** | | | **≈ 660 点/月** |

免费额度 **3000 点/月**，用了不到 1/4。

> ⚠️ **唯一没核实的计费项**：`watch()` 实时监听如何计费。官方只说明"数据库连接器调用次数 10 点/万次"，没写清长连接监听是否按变更次数计费。建议上线第一局后去「套餐用量页」看一眼实际消耗，再决定要不要放开用。

---

## 3. 免费版的限制，逐条核对你的场景

| 限制 | 对你的影响 |
|---|---|
| 自定义域名 **0 个** | ❗ 只能用默认域名。**符合你的要求（不买域名）** |
| 默认域名有「访问提示中间页」 | ⚠️ 好友**首次**访问会看到一个提示页，需点「确定访问」。点完 Cookie 有效期内不再出现。提前跟好友说一声 |
| `watch()` 连接数 **10** | ✅ 5 个好友每人 1 条 = 5 条。**注意：离开房间时必须关掉 watcher**，否则占着不放 |
| 云函数超时 **3s 且不可改** | ✅ 几次数据库操作远小于 3s |
| 日志保存 1 小时 | ✅ 出问题当天能看就行 |
| 环境 QPS 共享资源 | ✅ 5 人场景无压力 |
| 免费环境**不支持按量付费** | 🔴 **超额即停服**。所以必须盯着用量（见 §2，余量很大） |
| 不支持官网工单 | ⚠️ 出问题只能自己排查 |

---

## 4. 免费环境的两个真实风险（必须知道）

### 4.1 到期会被销毁，数据全丢

官方规则：免费环境单次**续费 6 个月**，**不支持自动续费**。到期进入停服隔离期，**未转付费将销毁环境**。

**对策**：
- 在日历里设提醒，每 5 个月续一次（到期前 1 个月内可续）
- 金瓜子和昵称**同时写 localStorage**，云端只作备份。这样即使环境被销毁，好友手机上的数据还在，重新建个环境导进去就行

### 4.2 默认域名有风控阈值

官方原话：「平台对默认域名访问量进行实时监控，若检测到访问量异常波动，平台保留采取禁止访问等风控措施的权利。」

3-5 个好友远低于任何阈值，不用担心。但如果以后想推广，就必须转付费 + 备案 + 绑域名。

---

## 5. 完整部署流程（你只需要做这几步）

### Step 1 — 创建环境（一次性）
1. 到 [CloudBase 购买页](https://buy.cloud.tencent.com/lowcode?buyType=tcb) 领**免费体验版兑换码**（一次性兑换码，每账号 1 个）
2. 用兑换码创建环境，地域选**上海**，数据库类型选**云数据库**
3. 记下 **环境 ID（envId）**

### Step 2 — 开两个开关（控制台点一下）
1. 「身份认证 → 登录方式」→ 开启**匿名登录**
2. 「安全来源」→ 确认默认域名已在白名单（文档说默认已含 `*.tcloudbaseapp.com`）

### Step 3 — 建集合
在「文档型数据库」建 `users` / `rooms` / `hands` 三个集合，按 §1 配安全规则。

### Step 4 — 部署前端
```bash
# 本地构建
npm run build

# 方式一：CloudBase CLI（推荐，可重复部署）
npm i -g @cloudbase/cli
tcb login                    # 用你自己的密钥登录，只在你本机
tcb hosting deploy dist -e <envId>

# 方式二：完全不想装 CLI
# 控制台「静态网站托管」→ 上传 dist 文件夹
```

### Step 5 — 部署云函数
```bash
tcb functions deploy -e <envId>
```

### Step 6 — 发给好友
把默认域名链接丢微信群。告诉好友：**首次打开有个提示页，点「确定访问」**。

---

## 6. 需要你提供什么

| # | 项目 | 说明 |
|---|---|---|
| 1 | **腾讯云账号** | 需实名认证。你自己注册 |
| 2 | **免费体验版兑换码** | 购买页领取，一次性 |
| 3 | **环境 ID** | 创建环境后控制台可见 |
| 4 | 开匿名登录 | 控制台点一下 |
| 5 | 建 3 个集合 + 配安全规则 | 控制台点几下，我给你配好的 JSON |

**🔴 密钥安全**：`tcb login` 用的 SecretId/SecretKey **只在你本机输入，不要发给任何人**。你也可以完全走控制台上传，不用 CLI。

**完全不需要**：域名、备案、Docker、Nginx、Redis、付费套餐。

---

## 7. 后续如果要升级（等好友真的在玩再说）

| 想做什么 | 要花什么 |
|---|---|
| 绑自己的域名（好看好记） | 域名 ~30-60 元/年 + ICP 备案 7-20 天 + 至少个人版 19.9 元/月 |
| 更强的防作弊 / 更稳定长连接 | 轻量服务器 2核2G 40 元/月起（此时代码只需换运行环境，逻辑不变） |
| 推广给更多人 | 同上 + 备案 |

**代码不用重写**：云函数 + 数据库的架构，和轻量服务器 + Express 的架构共享全部游戏逻辑（牌型判定、下注状态机、边池、结算），换的是"逻辑跑在哪"。

---

## 8. 技术栈最终确认（相对原需求的调整）

| 原需求 | 最终方案 | 原因 |
|---|---|---|
| Express + Socket.io 后端 | **云函数 + 文档型数据库 watch()** | 免费版云托管需按量付费、云函数 3s 超时，均跑不了 Socket.io。watch() 实时性对 5 人场景完全够 |
| Vercel Functions 适配 | **删除** | Serverless 无法保持 WebSocket 长连接，技术上不成立 |
| 内存存储房间数据 | **文档型数据库** | 内存存储重启即丢，且"永久累计金瓜子"与之矛盾 |
| Vue3 + Vite + GSAP + Vant | ✅ 保留 | 无问题 |
| qrcode 生成二维码 | ✅ 保留 | 房间二维码仍有用（同 WiFi 下扫码直接进） |
| 6 个 SVG 仓鼠头像 | ✅ 保留 | 程序化生成 |

---

## 9. 一句话

**0 元可行，方案已定：CloudBase 免费版 + 静态托管 + 云数据库 + 云函数跑游戏逻辑。你只需要注册腾讯云账号、领兑换码、开匿名登录，剩下的我来写。**
