# 数据上云 + 零费用部署方案（补充分析）

> 2026-09-20 补充 ｜ 回应两个问题：①数据能否存 CloudBase ②3-5 个好友扫码玩、不想有额外费用
> 配套文档：[可行性分析.md](可行性分析.md)（总体分析）

---

## 0. 先说结论

| 问题 | 结论 |
|---|---|
| 数据能存 CloudBase 吗 | ✅ **能，而且比你想的更彻底**——可以完全不要后端服务器，前端直连云数据库 |
| 零额外费用能实现吗 | ✅ **能，但只有一条路**：静态托管 + 文档型数据库 + 匿名登录，**不部署云托管** |
| 原需求的 Express+Socket.io 后端 | ⚠️ **在免费版上跑不起来**，最低要 19.9 元/月个人版 |
| 代价是什么 | 默认域名有访问提示中间页、watch 连接数上限 10、房主手机即"服务器"（可作弊、掉线即中断） |

---

## 1. 数据存云上：三层存储设计

CloudBase 文档型数据库（MongoDB 兼容）完全够用，按数据性质分三个集合：

### 1.1 用户层（金瓜子、昵称、头像）→ 集合 `users`

```js
{ _id: "<uid>", nickname: "仓鼠王", avatar: 3,
  goldenSeeds: 12, createdAt: 1726..., updatedAt: 1726... }
```

- **身份**：CloudBase **匿名登录**（控制台「身份认证 → 登录方式」开启），拿一个稳定 `uid`
- **安全规则**：`{"read": "doc._id == auth.uid", "write": "doc._id == auth.uid"}`，别人读不到你的金瓜子
- **本地缓存**：同时写 localStorage，打开页面先用本地值渲染，后台静默同步云端——断网也能玩
- **换设备**：匿名登录的 uid 存在浏览器 localStorage，**清缓存/换手机会变成新用户**。若要真正跨设备跟随，需改用「用户名密码」或「邮箱」登录（免费版也支持）

### 1.2 房间实时层 → 集合 `rooms`

```js
{ _id: "482913", mode: "online"|"offline", gameType: "long"|"short",
  initialSeeds: 3000, smallBlind: 10, bigBlind: 20, round: 3,
  status: "waiting"|"playing"|"settling",
  hostUid: "<uid>", members: [{uid, nickname, avatar, seeds, status}],
  // 线上模式公共状态
  communityCards: [...], pot: 480, currentBet: 40, turnUid: "<uid>", phase: "flop",
  updatedAt: 1726... }
```

- **实时同步**：所有玩家 `db.collection('rooms').doc(roomId).watch({onChange})`，任何变更推送到所有手机
- **费用**：`rooms` 更新一次 = 1 次数据库调用。一局德州 ~100 次写入 → 200点/万次 ≈ **0.002 元/局**
- **免费额度**：3000 点/月 ≈ **15,000 次写入/月**，对 3-5 个好友绰绰有余

### 1.3 手牌私密层 → 集合 `hands`

```js
{ _id: "<roomId>_<uid>", roomId: "482913", uid: "<uid>", cards: ["As","Kd"] }
```

- **安全规则**：`{"read": "doc.uid == auth.uid"}`，每人只能看到自己的底牌
- **为什么单独放**：如果把整副牌塞进 `rooms`，所有人 watch 一次就全看到了 → 直接作弊

### 1.4 这就是"不需要后端服务器"的架构

```
每个玩家的手机
   │  @cloudbase/js-sdk（匿名登录）
   ├──► rooms/{roomId}        watch() 实时订阅公共状态
   ├──► hands/{roomId}/{uid}  只读自己的底牌
   └──► users/{uid}           金瓜子读写
              ▲
              │ 房主手机 = 权威状态机（洗牌、发牌、下注、比牌、结算）
              │ 房主是唯一写 rooms 的人，其他人只写"动作请求"
```

**关键设计**：只有房主写 `rooms`，其他人只写 `hands` 和自己的动作请求。这样避免并发写冲突，不需要分布式锁。

---

## 2. 零费用可行性：两条路

### 2.1 先说清免费版的硬边界（新确认）

| 限制 | 原文依据 | 影响 |
|---|---|---|
| 免费版**不能开启按量付费** | 「免费环境暂不支持加购资源包和开启按量付费」 | 🔴 **致命** |
| 云托管**必须**开启超限按量，否则停止服务 | 「在使用云托管服务时，需要开启环境的超限按量服务……如果关闭了环境的超限按量，会导致被停止服务」 | 🔴 **免费版用不了云托管** |
| 免费版云函数超时 **3s 且不可改** | 套餐配额表：超时时间 免费版「3s，不支持修改」 | 🔴 WebSocket 云函数也废了 |
| 免费版自定义域名 **0 个** | 套餐配额表：自定义域名 免费版 = 0 | ⚠️ 只能用默认域名 |
| 免费版 watch 连接数 **10** | 套餐配额表：实时推送连接数 免费版 = 10 | ✅ 3-5 人够用 |
| 默认域名有访问提示中间页 | 「用户通过浏览器直接访问的请求将展示访问提示中间页」 | ⚠️ 好友首次访问多点一次"确定访问" |

**→ 结论：免费体验版上，`云托管 + Socket.io` 和 `云函数 WebSocket` 两条路都走不通。零费用只剩"纯前端 + 数据库"这一条。**

### 2.2 两条路对比

|  | **方案 A：纯前端 + 云数据库** | **方案 B：云托管 + Socket.io** |
|---|---|---|
| **月费用** | **0 元** | **19.9 元/月**（个人版） |
| 需要的套餐 | 免费体验版（3000 点/月） | 个人版起（40,000 点/月） |
| 后端代码 | **零**，只有 Vue 前端 | Express + Socket.io |
| 部署 | `tcb hosting deploy dist` 一条命令 | 还要构建镜像/传代码包 |
| 域名 | 仅默认域名（有中间页） | 默认域名 or 备案后自定义 |
| 实时机制 | `watch()` 订阅文档变更 | WebSocket 广播 |
| 连接数限制 | watch ≤ 10（免费版） | 无此限制 |
| 防作弊 | ❌ 房主手机即权威，房主可作弊 | ✅ 服务端权威 |
| 房主掉线 | 对局中断，需设计房主迁移 | 对局中断（内存态），需设计恢复 |
| 手机锁屏 | ⚠️ 浏览器后台节流定时器 | ✅ 服务端定时器正常跑 |
| 断线恢复 | 重连后重新 get 文档即可 | 需自己实现状态重建 |
| 需求符合度 | ⚠️ 偏离"Express+Socket.io"要求 | ✅ 完全符合原需求 |

### 2.3 我的建议

**先用方案 A 做出来跑通，0 元上线给好友玩；如果好友们真的喜欢，再花 19.9 元/月迁到方案 B。**

理由：
- 方案 A 的**部署简单到离谱**（只有一个静态目录），出问题你能自己改自己发
- 3-5 个熟人聚会场景，房主作弊没有意义，防作弊是伪需求
- 方案 A 的代码（状态机、牌型判定、结算）**可以 100% 复用到方案 B**，只是把"房主手机执行状态机"换成"服务端执行状态机"，前端几乎不用改
- 先验证"这游戏好不好玩"，再决定要不要花钱——避免花了钱做出来没人玩

---

## 3. 关于方案 B 的两个新发现（如果你选 B）

### 3.1 云托管强制无状态，且禁止后台活动

腾讯云官方原文（[服务开发说明](https://docs.cloudbase.net/run/develop/developing-guide)）：

> 「服务必须是**无状态服务**，不能依赖永久性本地状态。这是为了能够进行水平的自动扩缩容。」
> 「**避免后台活动**：服务处理完请求后，对应的容器实例**无法再访问 CPU**。因此，代码中请不要在请求处理程序范围之外启动后台线程或进程……强行运行后台线程可能会导致异常。」

对游戏服务端的影响：
- Socket.io 长连接存续期间实例是活跃的，官方 WebSocket 示例（Express + express-ws）就是这么跑的，**可用**
- 但游戏内的下注倒计时等 `setInterval` 定时器属于"请求处理范围之外的后台活动"，有被节流的风险
- **对策**：部署时选「**持续运行**」模式（固定实例副本数，官方明确该模式"不会自动扩缩容"），实例永不空闲，规避后台节流。3-5 人游戏单实例性能绰绰有余

### 3.2 云托管可以"只在玩的时候计费"（好消息）

官方已支持**定时扩缩容**（[运行模式与扩缩容](https://docs.cloudbase.net/run/deploy/configuring/autoscaling/about-instance-autoscaling)）：

- 「**自定义**」模式：自动扩缩容最小实例数 = 0、最大实例数 = 1，再加一条定时扩容规则（如每周六 20:00-23:00 保持 1 实例）
- 「**白天持续运行，夜间自动扩缩容**」：8:00-24:00 常驻，0:00-8:00 缩到 0
- 计费严格按实例运行秒数计量：「云托管计算资源：服务部署完成且实例启动后，实例**持续运行**所消耗的资源用量」

**含义**：聚会 3 小时就只付 3 小时的钱。个人版含 15 万 CU，最小规格（0.25 核 0.5GiB）1 实例约可跑 **34.7 天**——也就是说 19.9 元/月套餐里，云托管的用量**基本被额度覆盖，不额外产生费用**。

> ⚠️ 文档有一处自相矛盾：闲置回收时间，扩缩容页写「闲置 10 分钟」，简介页写「空闲 30 分钟」。按 10 分钟设计更安全。

### 3.3 网关 60 秒空闲断开（必须处理）

官方原文：

> 「如果 WebSocket/SSE 连接建立后长时间（**60s**）没有数据传输，网关会主动关闭非活跃的连接，建议客户端或者服务端**每隔 10s 发送一个心跳包**」

对策：Socket.io 默认 `pingInterval: 25s` 落在安全区，**不要改大**；客户端开启自动重连并显示"重连中"状态条。

---

## 4. 需求中后端技术错误的修正

原需求第 4 条：「生成 vercel.json 配置，前端可直接部署 Vercel，**后端适配 Vercel Functions**」

**这是错的，已直接修正。** Vercel Functions 是 Serverless 函数，**无法保持 WebSocket 长连接**，Socket.io 服务端无法在其上运行。

修正后的处理：
- `vercel.json` **保留**，但只用于前端静态部署（`outputDirectory: dist` + SPA rewrite）
- 后端明确写为「因 WebSocket 长连接限制，**不支持 Vercel Functions**」，部署目标为 CloudBase 静态托管 / 云托管 / Docker / 轻量服务器
- 如果你确实想用 Vercel 全栈，唯一路子是把实时推送从 WebSocket 改成 **SSE**（CloudBase HTTP 云函数默认支持 SSE，无需配置），但那是另一套架构，不建议在本项目引入

---

## 5. 需要你决策的（按重要性）

1. **选方案 A（0 元，无后端）还是方案 B（19.9 元/月，符合原需求）？**
   - 我的建议：**A 先跑通，B 作二期**。代码可复用，迁移成本低

2. **金瓜子要不要跨设备保留？**
   - A. 匿名登录，本机有效（0 成本，最简单）
   - B. 用户名密码/邮箱登录，换手机也在（免费版支持，但多一套注册登录流程）

3. **线上模式有人输光后？**
   - A. 自动重置全员，所有人复活继续（需求字面意思）
   - B. 该玩家淘汰观战，其余继续，直到剩一人

4. **短牌规则确认**：36 张（去 2-5）、三同花 > 同花顺、A 可高可低，对吗？短牌用 ante 还是大小盲注？

5. **是否实现边池（Side Pot）？** 多人多次全下必须做，否则筹码算错。建议做；若想省事可限制"每人最多全下一次"

6. **线下计分模式的操作粒度？**
   - "左边收瓜子 / 右边出瓜子"：操作自己的筹码，还是任意玩家可操作所有人？
   - 下注/弃牌在纯计分模式下具体做什么？

7. **域名与备案**（仅方案 B 正式上线需要）
   - 现在有没有已备案的域名？
   - 没有的话：先用默认域名给好友玩（有中间页，可接受），备案同步走（7-20 天）

---

## 6. 你需要提供什么（更新后）

| | 项目 | 方案 A（0元） | 方案 B（19.9元/月） |
|---|---|---|---|
| 1 | 腾讯云账号（实名认证） | ✅ 必需 | ✅ 必需 |
| 2 | **免费体验版兑换码** | ✅ 必需（[购买页](https://buy.cloud.tencent.com/lowcode?buyType=tcb) 领取，一次性兑换码，可续费 6 个月） | — |
| 3 | 环境 ID（envId） | ✅ 必需 | ✅ 必需 |
| 4 | 开启匿名登录 | ✅ 控制台点一下 | ✅ 控制台点一下 |
| 5 | 个人版套餐 | — | ✅ 19.9 元/月 |
| 6 | 域名 + ICP 备案 | 可跳过（用默认域名） | 🔴 正式上线必需，7-20 天 |
| 7 | 安全来源白名单配置 | ✅ 加你的域名 | ✅ 加你的域名 |

**🔴 密钥安全提醒**：部署只需你在控制台操作（或给仅部署权限的子账号），**不要把腾讯云主账号 SecretKey 发给任何人**。你也可以完全自己点控制台，我给你一条条命令。

---

## 7. 本次补充结论

1. **数据上云完全可行**，且 CloudBase 文档型数据库 + `watch()` 实时推送可以**完全替代自建 WebSocket 服务器**，这是零费用方案能成立的根本原因
2. **零额外费用可实现**：免费体验版 + 静态托管 + 数据库，月耗约几百资源点（3000 点额度内），**实际支出 0 元**
3. **代价**：默认域名有中间页、房主手机即权威、watch 连接数上限 10——对 3-5 人熟人聚会都可接受
4. **原需求的后端架构在免费版上不可行**（云托管需按量付费、云函数 3s 超时），最低 19.9 元/月
5. **Vercel Functions 的错误已修正**，WebSocket 长连接在 Serverless 上不成立

---

## 附：本次新增信息来源

- [运行模式与扩缩容](https://docs.cloudbase.net/run/deploy/configuring/autoscaling/about-instance-autoscaling)（定时扩容、闲置回收、实例销毁）
- [云托管简介](https://docs.cloudbase.net/run/introduction)（五种运行模式）
- [服务开发说明](https://docs.cloudbase.net/run/develop/developing-guide)（无状态要求、禁止后台活动）
- [使用 Websocket](https://docs.cloudbase.net/run/develop/access/websocket)（60s 空闲断开、10s 心跳）
- [使用限制](https://docs.cloudbase.net/run/limitation)（必须开启超限按量）
- [计费相关 FAQ](https://docs.cloudbase.net/run/faq/fee)（按实例运行时长计量）
- [计算资源使用量计量说明](https://cloud.tencent.com/document/product/876/120342)（CU 换算、个人版 15 万 CU）
- [资源点价格文档](https://cloud.tencent.com/document/product/876/127357)（套餐配额表：免费版 3s 超时、0 自定义域名、10 watch 连接）
- [默认域名访问限制及中间页](https://docs.cloudbase.net/service/alias)
- [数据库安全规则](https://docs.cloudbase.net/database/security-rules)（`get()` 跨文档校验）
- [匿名登录](https://docs.cloudbase.net/authentication-v2/method/anonymous)
- [文档型数据库](https://docs.cloudbase.net/database/introduce)
