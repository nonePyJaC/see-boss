# 切换修改清单 — PG 函数 → 阿里云服务器 + SQLite（线上线下统一）

> 2026-09-23 ｜ 执行清单，完成一项勾一项。
> 配套：[修缮清单.md](修缮清单.md)（通用修复；其中 1.2~1.5 并入本清单 §4 一起做）
> ⚠️ 部署目标已从「腾讯云托管」改为「阿里云服务器 + SQLite」，CloudBase（PG/静态托管/云函数/JS SDK）整体退役。

---

## 进度记录

### 2026-09-24 ｜ 实测四轮修复（181 测试全绿）

真机实测反馈的 4 个问题已全部修完：

1. **盲注**：无推荐档位；填一个自动算另一个（×2/÷2）；前后端双重边界校验
   （正值 / 大麦>小麦 / 大麦偶数 / 大麦 ≤ 入场÷10）。`server/index.js` `newRoom`
   + `OfflineEntryView.vue` `onBlindInput`/`settingError`；顺手修了一个残留 bug：
   watch 里引用了已删除的 `BLIND_PRESETS`，换入场数会抛 ReferenceError。
2. **五颗花生（river）还有一轮下注**：河牌轮下注键保持可用，全 check/平注后才等收池；
   全员 all-in 的死街自动连亮到河牌（`afterMove` 的 `while advanced && !turnUid` 循环）。
3. **暂停分池**：「出」= 投进公共池不指定接收人、「收」= 从池子拿走；
   继续时分流——池空 → `needConfirmNextHand` 房主确认才开新一轮；
   池有值 → 回滚 `pauseBackup`（= 最后一次行动前的 `prevState`）恢复 B 决策。
   ⚠️ pauseBackup 必须在置 `paused=true` **之前**克隆，否则回滚把 paused 也带回去。
4. **非房主结算蒙层卡死**：`pull()` 里 `settleOpen = !!settlePending` 双向同步。
5. **all-in**：`raise` 超出瓜子钳制为全下不报错；短筹全下跟注（不够平注）不重置行动轮；
   `avail.call` 永远 enabled 并带 `allIn` 标记；前端加注弹窗 `raiseMax<=0` 时直接 call 全下。

回归：`shared/logic` 139/139 + `scripts/test-room-server` 42/42 + 前端 `vite build` 通过。

### 此前完成（见 git log）

服务端 `/api/*` 全套、SQLite 存储层、快照恢复、离场=弃牌、Dockerfile 多阶段
（node:24-alpine）、`.dockerignore`、前端 `room-repo.js` 接线、调试页迁移等。

---

## 0. 一句话目标

**一台阿里云服务器跑一个 Node 进程 = 静态站点 + 房间 API + SQLite 存储；两种房间模式（offline / online）共用同一引擎；CloudBase 全部退役。**

```
手机浏览器（同服务器返回的静态页）
   │
   └──► 阿里云服务器 · Node 进程（serve client/dist + /api/*）
          ├── 房间状态机（内存 + SQLite 快照 → 重启自动恢复）
          ├── 账号 / 金瓜子账本 / 历史（SQLite）
          └─ 共用 shared/logic 引擎（80/80 测试）
```

核心变化：
1. **房间状态机离开 SQL**——`move_pot` / `rotate_blinds` / `room_snapshot` / `offline_settle` 等 PG 函数全部退役，引擎逻辑只存在 `shared/logic` 一份。
2. **存储从 CloudBase PG 换成服务器本地 SQLite**——账号、账本、历史、房间快照都进一个 `.db` 文件；备份 = 拷文件。
3. **前端不再依赖 CloudBase JS SDK**——全部改走自家 `/api/*`；身份用 localStorage 里的 uid + 昵称（朋友局，够用）。

---

## 1. 新增需求

> 格式：`- [ ] 需求描述 ｜ 落点：引擎/快照的哪个字段`
> 以下 1~4 组为 2026-09-23 补充，细化并取代最早的「收/call/raise/弃牌」「下一家高亮」两条。

### 1.1 公共池区域
- [ ] 公共池下方展示改为：**每位玩家的下注瓜子数量**（当前注额条）
      ｜ 落点：快照 `seats[].bet`（已有），UI 新增注额条
- [ ] **点击公共池 → 弹窗展示下注瓜子流水**
      ｜ 落点：房间状态新增 `betLog`（记 sb/bb/跟/倍/收 事件；收款后清空，沿用现行行为）

### 1.2 操作按钮改为 收 / 跟 / 弃（「倍」并入「跟」）
- [ ] **收**：点击收走公共池内所有瓜子
      ｜ 落点：新动作 `collect`；收完后判断是否有人 0 瓜子（见 §1.3）
- [ ] **跟**：单击 = 平跟（自动补齐到与当前注额一致）；长按 = 加注弹窗
      ｜ 落点：单击 `applyAction({type:'call'})`（金额 = `currentBet − 我的bet`，按钮上显示需跟数）；
      长按弹窗 → `applyAction({type:'raise', amount: currentBet + 加倍数量})`
      - 弹窗顶部：需跟注数量；下方快捷加倍 10 / 20 / 50 / 100 / 300 / 500 + 自定义加倍输入
      - 语义 = 跟注 + 额外加注：上家下注 100 → 弹窗显示「需跟 100」→ 点 50 → 我这手共下 150
      - 加注上限 = 全下，前端校验
- [ ] **弃**：点击二次确认；确认后轮询不再触发该玩家，直到下一手开始
      ｜ 落点：`applyAction({type:'fold'})` + 前端确认弹窗；引擎新一手自动重置 folded
- [ ] **只剩 1 名未弃牌玩家时，该玩家自动触发「收」**
      ｜ 落点：服务端 fold 后判定存活人数 → 自动 collect → 接 §1.3 判断

### 1.3 结算机制（收款 → 下一手大小麦之间）
- [ ] 收款后判断：**有玩家 0 瓜子** → 只对房主弹窗、其他玩家蒙层等待；**无人 0 瓜子** → 不弹窗，正常大小麦轮转直接进下一手
      ｜ 落点：`roundStatus = 'settle-pending'`（仅有人归零时进入）
- [ ] 弹窗【结算并重置】：0 瓜子玩家各给筹码最高者 1 粒金瓜子 + 重置全员瓜子回初始值 + 清空大小麦麦位（房主重新设置）
- [ ] 弹窗【结算并解散】：金瓜子结算 + 全员离开房间（销毁）
- [ ] 弹窗右上角 **X**：不做任何操作、不触发大小麦；公共池左侧显示【暂停】文案，等待玩家自主操作
      ｜ 专用于「大家 all-in 归零、最后又平分继续」的特殊场景（如 AB all-in，河牌后 A 先收走、再手动发一半给 B）
      ｜ 落点：`roundStatus = 'paused'`（手动模式：无自动大小麦，出/收自由）
- [ ] 房主点击【暂停】→ 弹窗【继续】/【结算重置】：
      - 继续 → 触发下一手大小麦，恢复对局（筹码沿用，不重置）
      - 结算重置 → 同【结算并重置】

### 1.4 决策高亮动效
- [ ] 轮询到谁决策，该玩家**头像闪烁/高亮**
      ｜ 落点：快照 `turnUid` + CSS 动效（`OnlineRoomView` 已有 `isTurn` 样式，升级为闪烁）

- [ ] （继续补充）

---

## 2. 已确认口径（2026-09-23 定稿）

- [x] **弹窗时机**：任意玩家点「收」收走公共池后，判断是否有玩家瓜子为 0——**有**才给房主弹窗、其他玩家蒙层；**没有**则不弹窗，正常大小麦轮转进下一手
- [x] **「倍」并入「跟」**：只保留三个按钮，中间按钮 =【跟】——单击平跟、长按弹加注窗
- [x] **新一手开始**：无归零 → 正常大小麦轮转；有归零 + 房主点「结算重置」→ 重置后开始新一轮
      手内不区分翻牌/转牌/河牌：一整手 = 一轮连续下注（加注即重置行动轮，换街后的下注用长按【跟】表达）
- [x] **加注上限**：最多全下，前端校验
- [x] **结算规则**：0 瓜子玩家各给「筹码最高者」1 粒金瓜子（并列最高沿用现行：房主点选）
- [x] **重置范围**：全员瓜子回初始值 + 清空大小麦麦位（房主重新指定谁小麦位）
- [x] **普通继续不重置**：X → 暂停 → 继续 后筹码沿用；暂停专用于「大家 all-in 归零、最后又平分继续」的特殊场景

### 待确认（阿里云版新增）
- [ ] SQLite 取代 **全部** CloudBase 数据（账号、账本、历史、房间快照）——PG 完全不用了？
- [ ] 现有 CloudBase 里的账号/金瓜子要不要迁过来？（朋友局可直接重建；要迁就写个一次性导出脚本）
- [ ] 房间号池：线上线下共用一个 6 位号池（同一个 Map，`mode` 字段区分）——默认按共用做

---

## 3. 仓库与部署（GitHub → 阿里云服务器）

### 3.1 推送前仓库收拾

- [ ] `.gitignore` 补 `dist.zip`、`test-out.txt`、`server/data/*.db*`；中文注释重写为 UTF-8（现为 GBK 乱码）
- [ ] 根目录过期 `package-lock.json`（还是 `hamster-poker`+`concurrently`）重生成或删除
- [ ] `cloud-functions/` 整个目录退役（`hello` / `pg-probe` / `pg-probe2` / `whoami` 都不再需要）
- [ ] 删 `/testroom` 路由 + `TestRoomView.vue`
- [ ] `client` 移除 `@cloudbase/js-sdk` 依赖（见 §5）
- [ ] `git init` → 首次提交 → 推 GitHub

### 3.2 阿里云服务器准备

- [ ] 服务器：ECS / 轻量应用服务器均可，1C1G 起步绰绰有余
- [ ] 装 Node 20+（`nvm` 或官方源）+ `pm2`（`npm i -g pm2`，`pm2 startup` 开机自启）
- [ ] 安全组放行端口：HTTP 80（或先 8080 测试）
- [ ] 数据目录：`server/data/hamster.db`（SQLite 文件；写个 cron 每天拷一份即完成备份）

### 3.3 部署方式（二选一）

| 方式 | 流程 | 适用 |
|---|---|---|
| **A. 直接跑（推荐先上）** | 服务器 `git clone` → `cd client && npm ci && npm run build` → `pm2 start server/index.js`；更新 = `git pull && 重 build client && pm2 restart` | 最快，无额外组件。⚠️ 服务器 Node 必须 ≥22.13（建议 24，用户已装） |
| B. Docker | `docker build -t hamster . && docker run -d -p 80:80 -v hamster-data:/app/server/data hamster`；Dockerfile 已是多阶段（构建 client → node:24 运行时） | 环境隔离更干净 |

- [ ] Node 进程兼做静态服务：`server/index.js` 增加 serve `client/dist`（约 30 行，无需 nginx）；API 与页面同域，天然免 CORS
- [ ] 域名/HTTPS：朋友局可先 `IP:端口` 用着；要域名再补备案 + Caddy/nginx

---

## 4. 服务端改造（`server/`）

### 4.1 存储层（`server/db.js`）✅ 已完成

- [x] SQLite 驱动：**`node:sqlite`（Node ≥22.13 内置）**——零 npm 依赖、零编译，比 better-sqlite3 更省事
- [x] 建表：`accounts` / `account_ledger`（owner+peer 唯一累加）/ `history` / `room_snapshots`
- [x] 账号 API：`login` / `me` / `update` / `logout` / `ledger` / `clear` / `history`——accounts.sql 的 plpgsql 已直译为 JS，**并修了三个线上事故根因**（双方同时冲销、不删归零行、余额不为负）
- [x] 身份：请求体带 `uid` + 昵称，服务端信任
- [x] 解绑改 UPDATE tombstone（不再 DELETE，否则 CASCADE 把对方账本行也抹掉）

### 4.2 房间状态机（`server/index.js` + `shared/logic/offline-room.mjs`）✅ 已完成

- [x] 房间模型 `mode: 'offline' | 'online'`；offline 走独立引擎 `offline-room.mjs`（不发牌、花生节拍器 stage）
- [x] `publicState` 输出 `toCall` + `avail`（动态按钮：欠注只给跟/倍/弃，平注才出过）
- [x] 动作：`collect`（不占回合，谁都收）/ `call` / `raise`（amount=需跟+加倍，上限全下）/ `check` / `fold` / `give`（paused 专用划拨）
- [x] **弃牌不分回合**（2026-09-24 定稿）：离场=弃牌，随时可弃；非回合弃牌不抢当前行动者回合
- [x] 收款后判断：有人 0 瓜子 → `settlePending`（透出到 publicState）；无人 → 正常轮转
- [x] fold 后只剩 1 人 → 自动 `collect`
- [x] 结算服务端直写账本（不再经客户端转发），幂等 = finished+有人归零
- [x] 重置 = 瓜子回初始 + 清麦位 + **`state=null`**（等房主重新开局；旧方案留 finished=false 的重置态会把 /start 卡死）
- [x] `betLog` = `actionLog`（下注流水，收款清空）
- [x] 快照：每次变更 upsert + 启动载回（>24h 陈旧快照丢弃）；`pm2 restart` 自动恢复
- [x] 顺手修掉：handleLeave 毁房、断线重连、房间清扫+MAX_ROOMS、入参校验/roundNo/请求体上限/crypto 洗牌
- [x] 本轮新修（2026-09-24 review）：settlePending/paused 透出、sbIndex 透传、房主离开删快照、轮询不落库、快照时效过滤、isMain/serveStatic 修正

---

## 5. 前端改造

- [ ] **去 CloudBase**：删 `@cloudbase/js-sdk` 依赖、`cloudbase-init`、`cloud-repo.js`、`user.js`/`accounts.js` 里所有 SDK 调用；改走 `fetch('/api/*')`
- [ ] 身份：`localStorage` 生成/读取 `uid` + 昵称（进入时输入一次即可）
- [ ] 新增 `client/src/data/room-repo.js`（统一 online/offline）：
      `create / join / start / action(call|raise|fold|allin) / collect / settle / state / leave` + 2s 轮询
- [ ] 新增 `client/src/data/account-repo.js`：账号/账本/历史走自家 API
- [ ] `OnlineRoomView.vue` 重写：删本地状态机与 bot，全部走 room-repo
- [ ] `OfflineRoomView.vue`：数据层替换为 room-repo（UI 保留），删 `repo.*` RPC 调用
- [ ] 共用组件 `ActionBar.vue`：**收 / 跟 / 弃** 三键
      跟：按钮显示需跟金额；单击平跟；长按弹加注窗（顶部需跟数量 + 快捷加倍 10/20/50/100/300/500 + 自定义加倍，上限全下前端校验）；弃：二次确认
- [ ] 公共池区：下方注额条（每人当前 bet）+ 点击弹「下注流水」弹窗（读 `betLog`）
- [ ] 结算弹窗（仅有人 0 瓜子时出现；房主）：【结算并重置】【结算并解散】+ 右上 X；非房主蒙层等待
- [ ] 【暂停】文案（公共池左侧，房主可点）→【继续】/【结算重置】弹窗
- [ ] 决策动效：`turnUid` 头像闪烁/高亮，两个房间页共用
- [ ] 双账本修复（修缮清单 2.2）：旧 `users`/`seed_ledger` 路径直接随 CloudBase 退役消失，问题自愈
- [ ] `.env` 瘦身：`VITE_DATA_SOURCE` / `VITE_CLOUDBASE_*` / `VITE_REALTIME_MODE` 全删；只留可选的 `VITE_API_BASE`（默认同域，部署时基本不用配）

---

## 6. 数据持久与恢复（SQLite）

- [ ] 房间快照：`room_snapshots` 表由**服务端**写（每次动作后 upsert），重启时载回——比云托管方案少一整条「客户端写 PG + restore API」链路
- [ ] 账本：`account_ledger` 写入与结算同事务
- [ ] 备份：cron `cp data/hamster.db data/hamster-$(date +%F).db`，保留 7 天
- [ ] 迁移（可选）：CloudBase `accounts`/`account_ledger` 导出 CSV → 脚本导入 SQLite；不迁就让大家重建账号

---

## 7. CloudBase 善后

- [ ] **退役**：PG 全部函数与表（含 `accounts`/`account_ledger`/`history`，逻辑已搬进 SQLite）；静态托管；云函数目录；JS SDK
- [ ] 文件归档：`db/` 目录整体挪到 `db/archive/` 保留（盲注轮转规则、结算规则有参考价值），不删
- [ ] CloudBase 环境：确认不再使用后可直接释放，省得惦记免费额度到期
- [ ] 文档更新：`docs/线上模式任务拆分.md`、`补充逻辑文档.md` 等里所有 CloudBase/PG/云函数描述标注废弃

---

## 8. 验收（真机）

- [ ] 两台手机进同一房间，快照同步正常
- [ ] 回合制：跟按钮显示需跟金额、单击平跟、长按加注（100+50=150，上限全下）、弃二次确认、决策头像闪烁
- [ ] 只剩 1 人未弃 → 自动收池
- [ ] 收款后：有人 0 瓜子 → 房主弹结算弹窗、非房主蒙层；无人 0 → 直接正常轮转下一手
- [ ] X → 出现【暂停】；暂停 → 继续（筹码沿用）/ 结算重置（回初始 + 清空麦位）生效
- [ ] 公共池下方注额条 + 点击弹下注流水
- [ ] 断线重连：对局中刷新页面能回到座位
- [ ] 结算对账：账本 +1 正确、不重复
- [ ] **重启恢复**：`pm2 restart` 或重启服务器 → 重进房间 → 对局状态自动回来（SQLite 快照）
- [ ] 静态页 + API 同域访问正常（`IP:端口` 直接打开就能玩）

## 9. 风险与回滚

| 风险 | 缓解 |
|---|---|
| 单点服务器宕机 | pm2 自动拉起 + SQLite 快照恢复；朋友局可接受 |
| SQLite 文件损坏/误删 | cron 每日备份；房间快照表可重建（最坏丢一局进行中的牌） |
| `better-sqlite3` 编译失败 | 服务器装 `build-essential`；备选 Node 22 `node:sqlite` 或换 `sql.js`（纯 JS，性能足够） |
| 部署更新打断对局 | 打牌时段不 `pm2 restart`；重启恢复快照兜底 |
| 新架构上线后发现问题 | `db/` 只归档不删，CloudBase 环境先留着，必要时可临时回切 |
