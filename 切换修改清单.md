# 切换修改清单 — PG 函数 → 云托管容器（线上线下统一）

> 2026-09-23 ｜ 执行清单，完成一项勾一项。
> 配套：[修缮清单.md](修缮清单.md)（通用修复；其中 1.2~1.5 并入本清单 §4 一起做）

## 0. 一句话目标

**一个云托管容器（GitHub 自动构建）= 两种房间模式（offline / online）；前端一份 room-repo 驱动两个房间页；PG 只留账号、金瓜子账本、历史。**

```
手机浏览器（静态托管）
   │
   ├── 房间实时状态（线上 + 线下）──► 云托管容器（内存态 + PG 快照兜底）
   │                                  └─ 共用 shared/logic 引擎（80/80 测试）
   │
   └── 账号 / 金瓜子账本 / 历史 ─────► CloudBase PG
                                        accounts / account_ledger / history
```

核心变化：**房间状态机离开 SQL**。`move_pot` / `rotate_blinds` / `room_snapshot` / `offline_settle` 等函数全部退役，引擎逻辑只存在 `shared/logic` 一份。

---

## 1. 新增需求

> 格式：`- [ ] 需求描述 ｜ 落点：引擎/快照的哪个字段`
> 以下 1~4 组为 2026-09-23 补充，细化并取代最早的「收/call/raise/弃牌」「下一家高亮」两条。

### 1.1 公共池区域
- [ ] 公共池下方展示改为：**每位玩家的下注瓜子数量**（当前注额条）
      ｜ 落点：快照 `seats[].bet`（已有），UI 新增注额条
- [ ] **点击公共池 → 弹窗展示下注瓜子流水**
      ｜ 落点：房间状态新增 `betLog`（记 sb/bb/跟/倍/收 事件；收款后清空，沿用现行行为）

### 1.2 操作按钮改为 收 / 跟 / 弃（「倍」并入「跟」）
- [ ] **收**：点击收走公共池内所有瓜子
      ｜ 落点：新动作 `collect`；收完后判断是否有人 0 瓜子（见 §1.3）
- [ ] **跟**：单击 = 平跟（自动补齐到与当前注额一致）；长按 = 加注弹窗
      ｜ 落点：单击 `applyAction({type:'call'})`（金额 = `currentBet − 我的bet`，按钮上显示需跟数）；
      长按弹窗 → `applyAction({type:'raise', amount: currentBet + 加倍数量})`
      - 弹窗顶部：需跟注数量；下方快捷加倍 10 / 20 / 50 / 100 / 300 / 500 + 自定义加倍输入
      - 语义 = 跟注 + 额外加注：上家下注 100 → 弹窗显示「需跟 100」→ 点 50 → 我这手共下 150
      - 加注上限 = 全下，前端校验
- [ ] **弃**：点击二次确认；确认后轮询不再触发该玩家，直到下一手开始
      ｜ 落点：`applyAction({type:'fold'})` + 前端确认弹窗；引擎新一手自动重置 folded
- [ ] **只剩 1 名未弃牌玩家时，该玩家自动触发「收」**
      ｜ 落点：服务端 fold 后判定存活人数 → 自动 collect → 接 §1.3 判断

### 1.3 结算机制（收款 → 下一手大小麦之间）
- [ ] 收款后判断：**有玩家 0 瓜子** → 只对房主弹窗、其他玩家蒙层等待；**无人 0 瓜子** → 不弹窗，正常大小麦轮转直接进下一手
      ｜ 落点：`roundStatus = 'settle-pending'`（仅有人归零时进入）
- [ ] 弹窗【结算并重置】：0 瓜子玩家各给筹码最高者 1 粒金瓜子 + 重置全员瓜子回初始值 + 清空大小麦麦位（房主重新设置）
- [ ] 弹窗【结算并解散】：金瓜子结算 + 全员离开房间（销毁）
- [ ] 弹窗右上角 **X**：不做任何操作、不触发大小麦；公共池左侧显示【暂停】文案，等待玩家自主操作
      ｜ 专用于「大家 all-in 归零、最后又平分继续」的特殊场景（如 AB all-in，河牌后 A 先收走、再手动发一半给 B）
      ｜ 落点：`roundStatus = 'paused'`（手动模式：无自动大小麦，出/收自由）
- [ ] 房主点击【暂停】→ 弹窗【继续】/【结算重置】：
      - 继续 → 触发下一手大小麦，恢复对局（筹码沿用，不重置）
      - 结算重置 → 同【结算并重置】

### 1.4 决策高亮动效
- [ ] 轮询到谁决策，该玩家**头像闪烁/高亮**
      ｜ 落点：快照 `turnUid` + CSS 动效（`OnlineRoomView` 已有 `isTurn` 样式，升级为闪烁）

- [ ] （继续补充）

---

## 2. 已确认口径（2026-09-23 定稿）

- [x] **弹窗时机**：任意玩家点「收」收走公共池后，判断是否有玩家瓜子为 0——**有**才给房主弹窗、其他玩家蒙层；**没有**则不弹窗，正常大小麦轮转进下一手
- [x] **「倍」并入「跟」**：只保留三个按钮，中间按钮 =【跟】——单击平跟、长按弹加注窗
- [x] **新一手开始**：无归零 → 正常大小麦轮转；有归零 + 房主点「结算重置」→ 重置后开始新一轮
      手内不区分翻牌/转牌/河牌：一整手 = 一轮连续下注（加注即重置行动轮，换街后的下注用长按【跟】表达）
- [x] **加注上限**：最多全下，前端校验
- [x] **结算规则**：0 瓜子玩家各给「筹码最高者」1 粒金瓜子（并列最高沿用现行：房主点选）
- [x] **重置范围**：全员瓜子回初始值 + 清空大小麦麦位（房主重新指定谁小麦位）
- [x] **普通继续不重置**：X → 暂停 → 继续 后筹码沿用；暂停专用于「大家 all-in 归零、最后又平分继续」的特殊场景

### 仍未定
- [ ] 房间号池：线上线下共用一个 6 位号池（同一个 Map，`mode` 字段区分）

---

## 3. 仓库与部署（GitHub → 云托管）

### 3.1 推送前仓库收拾

- [ ] `.gitignore` 补 `dist.zip`、`test-out.txt`；中文注释重写为 UTF-8（现为 GBK 乱码）
- [ ] 根目录过期 `package-lock.json`（还是 `hamster-poker`+`concurrently`）重生成或删除
- [ ] 删 `cloud-functions/` 探针：`hello` / `pg-probe` / `pg-probe2`（`whoami` 留作联调）
- [ ] 删 `/testroom` 路由 + `TestRoomView.vue`
- [ ] `git init` → 首次提交 → 推 GitHub

### 3.2 云托管「新建 Git 平台部署」表单

| 字段 | 填法 | 说明 |
|---|---|---|
| Git 仓库 | 新建的 GitHub 仓库 + `main` 分支 | |
| 启用自动部署 | **先关** | 开着每次 push 都重建实例，进行中的房间会中断；稳定后再开 |
| 服务名称 | `hamster-room` | 小写 + 连字符，会进默认域名 |
| 访问端口 / 服务端口 | `80` / `80` | 与 Dockerfile `EXPOSE 80`、`process.env.PORT \|\| 80` 一致 |
| 构建设置 | Dockerfile 路径默认 | 根目录 Dockerfile 只 COPY `server/` + `shared/`，镜像很小 |
| 环境变量 | 不用加 | 服务零依赖零配置 |
| 更多配置 | 规格 0.25核0.5GiB | 健康检查路径填 `/health` |

### 3.3 创建后必须调

- [ ] **副本模式切「低成本」：最小副本 0、最大副本 1**
      最小 0 → 不烧免费额度（常驻 1 实例 ≈ 21,717 点/月，超 3000 点额度 7 倍）
      最大 1 → 内存态硬约束，多实例会「房间随机不存在」
- [ ] 实测冷启动：缩容到 0 后第一次请求耗时（最长 30s，通常 3~10s）→ 前端进房配 loading + 重试
- [ ] 拿默认域名 → 前端 `VITE_ROOM_API_BASE`

---

## 4. 服务端改造（`server/index.js`）

- [ ] 房间模型加 `mode: 'offline' | 'online'`；offline 不洗牌、不发底牌、不摊牌
- [ ] `publicState` 输出 `toCall`（`currentBet - 我的bet`）——call 按钮金额直接用
- [ ] 动作接口对齐 §1.2：`collect` / `call` / `raise`（长按跟，amount = currentBet + 加倍数量，上限全下）/ `fold`
- [ ] 收款后判断：有人 0 瓜子 → `settle-pending`（房主弹窗）；无人 → 正常大小麦轮转进下一手
- [ ] fold 后判定存活人数，只剩 1 人时自动 `collect`（接同一判断）
- [ ] 结算：0 瓜子玩家各付「筹码最高者」1 粒 → transfers 返回房主客户端 → 调 `account_seed_transfer`
- [ ] 重置范围：全员瓜子回初始值 + 清空大小麦麦位
- [ ] 房间状态新增 `betLog`（下注流水，收款后清空；公共池弹窗用）
- [ ] 手内连续下注：不建 street 概念（加注即重置行动轮）
- [ ] 快照写入 / 恢复接口（见 §6）
- [ ] 顺手修掉（源自修缮清单）：1.2 `handleLeave` 毁房 bug、1.3 `handleJoin` 断线重连、1.4 房间清扫 + `MAX_ROOMS`、1.5 入参校验 / `roundNo` / 请求体上限 / crypto 洗牌

## 5. 前端改造

- [ ] 新增 `client/src/data/room-repo.js`（统一 online/offline）：
      `create / join / start / action(call|raise|fold|allin) / collect / nextStreet / settle / state / leave` + 2s 轮询
- [ ] token：`localStorage["credentials_<envId>"]` → `Authorization: Bearer`
- [ ] `OnlineRoomView.vue` 重写：删本地状态机与 bot，全部走 room-repo
- [ ] `OfflineRoomView.vue`：数据层替换为 room-repo（UI 保留），删 `repo.*` RPC 调用
- [ ] 共用组件 `ActionBar.vue`：**收 / 跟 / 弃** 三键
      跟：按钮显示需跟金额；单击平跟；长按弹加注窗（顶部需跟数量 + 快捷加倍 10/20/50/100/300/500 + 自定义加倍，上限全下前端校验）；弃：二次确认
- [ ] 公共池区：下方注额条（每人当前 bet）+ 点击弹「下注流水」弹窗（读 `betLog`）
- [ ] 结算弹窗（仅有人 0 瓜子时出现；房主）：【结算并重置】【结算并解散】+ 右上 X；非房主蒙层等待
- [ ] 【暂停】文案（公共池左侧，房主可点）→【继续】/【结算重置】弹窗
- [ ] 决策动效：`turnUid` 头像闪烁/高亮，两个房间页共用
- [ ] 结算链路：`room-repo.settle()` 拿 transfers → 调 `account_seed_transfer`
- [ ] 双账本修复（修缮清单 2.2）：删 `payGoldenSeeds` / `recordSeed` 旧路径
- [ ] `cloud-repo.js` 瘦身：只留账号 / 账本 / 历史，删房间相关全部方法

## 6. 快照机制（容器缩容/重启兜底）

**表**（PG，新建）：

```sql
CREATE TABLE IF NOT EXISTS public.room_snapshots (
  room_id    varchar(6) PRIMARY KEY,
  mode       varchar(8)  NOT NULL DEFAULT 'offline',
  state      jsonb       NOT NULL,   -- {players, pot, turnUid, roundNo, blinds, dealerUid,
                                     --  roundStatus: playing | settle-pending | paused, betLog}
  updated_at timestamptz NOT NULL DEFAULT now()
);
-- RLS：is_room_member(room_id, auth.uid()) 可读写；退出房间时删行
```

- [ ] 写入：房主客户端每 10s + 每次动作成功后 upsert（一次小请求，成本可忽略）
- [ ] 恢复：join 报「房间不存在」→ 查快照（<24h）→ 提示「恢复上次进度」→ 调容器 `/api/room/restore` 带快照重建
- [ ] 清理：结算退出删快照行

## 7. PG 侧善后

- [ ] **退役**（函数 + 表，文件归档到 `db/archive/`，库里先不删）：
      `move_pot` / `rotate_blinds` / `set_blinds` / `reset_blinds` / `reorder_seats` / `room_snapshot` / `set_auto_blind` / `offline_settle` / `list_joinable_rooms` / `leave_online_room` / `cleanup_stale_rooms`
      `rooms` / `room_members` / `pot_log` / `hands` / `decks`
- [ ] **保留**：`accounts.sql` 全套 + `account_seed_transfer` / `account_ledger_rows` / `clear_account_ledger_row` + `history`
- [ ] 归档时把「盲注轮转规则」「结算规则」抄进本清单备注，防止将来行为漂移

## 8. 验收（真机）

- [ ] 两台手机进同一房间，快照同步正常
- [ ] 回合制：跟按钮显示需跟金额、单击平跟、长按加注（100+50=150，上限全下）、弃二次确认、决策头像闪烁
- [ ] 只剩 1 人未弃 → 自动收池
- [ ] 收款后：有人 0 瓜子 → 房主弹结算弹窗、非房主蒙层；无人 0 → 直接正常轮转下一手
- [ ] X → 出现【暂停】；暂停 → 继续（筹码沿用）/ 结算重置（回初始 + 清空麦位）生效
- [ ] 公共池下方注额条 + 点击弹下注流水
- [ ] 断线重连：对局中刷新页面能回到座位
- [ ] 结算对账：账本 +1 正确、不重复
- [ ] 缩容恢复：等 30 分钟无流量（或手动缩容）→ 重进房间 → 恢复提示生效
- [ ] 冷启动体验：首屏 loading + 重试不白屏

## 9. 风险与回滚

| 风险 | 缓解 |
|---|---|
| 低成本模式冷启动最长 30s | 前端 loading + 自动重试；实测后如无法接受再议常驻 |
| 部署/重启打断进行中的对局 | 快照兜底；打牌时段不 push |
| 新架构上线后发现问题 | PG 旧函数只归档不删，必要时可临时回切 |
| 自动部署误触发 | 先关，手动发布 |
