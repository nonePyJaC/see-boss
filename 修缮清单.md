# 修缮清单 — 上线前

> 2026-09-23 全量审查后整理。
> **范围约定**：纯私人朋友局，数字记录无实际意义。本清单只收【功能性】【性能】【部署可跑】三类问题。
> JWT 验签、CORS 白名单、RLS 列权限、账号口令、防篡改等安全项已评估并**明确不修**（见文末「不做清单」）。

## 0. 现状速览

- **线上对局** = `server/index.js`（云托管容器，全内存，退出即焚）→ **前端还没接，部署上去没人会调**
- **线下联机** = CloudBase PG + RPC 函数（当前生产路径，OfflineRoomView 在用）
- 两条路并存没问题，但 PG 侧有一批账本/函数版本漂移问题要收

---

## 1. 线上对局服务（`server/index.js`）

### 1.1 🔴 前端未接入 —— 不上线原因之首

`OnlineRoomView.vue` 是纯本地演示（`addBot` 机器人 + 本地引擎），全项目没有代码调 `/api/online/*`。

- [ ] 新建 `client/src/data/online-repo.js`：封装 create/join/start/action/state/leave + 2s 轮询（复用 `realtime.js` 的 `createRoomSync` 思路）
- [ ] `.env.example` 和 `client/.env` 加 `VITE_ONLINE_API_BASE=`（云托管域名）
- [ ] token 来源：`localStorage["credentials_<envId>"]` 里的 `access_token`（`scripts/jwt-relay.mjs:10` 同款取法）
- [ ] `OnlineRoomView.vue` 重写：本地 ref 全部换成服务端快照驱动（视觉可复用）

### 1.2 🔴 `handleLeave` 会毁掉整局 —— `server/index.js:275-279`

非房主在**不是自己回合**离开时，`applyAction` 返回 `{error}`（无 `state` 字段），代码无条件 `room.state = r.state` → **牌局状态被置 undefined**：phase 变 IDLE、外人可加入、可重复开局、底池消失。手机切后台/关页面必然踩到。

- [ ] 引擎加一个绕过回合校验的 `forfeit(state, uid)`（标记弃牌 + 推进终局判定）
- [ ] leave 路径复用 `handleAction` 里的摊牌结算块（fold 终局时底池要发下去）

### 1.3 🔴 `handleJoin` 断线重连被误杀 —— `server/index.js:145`

「对局已开始」判断在「是否已在座」之前 → 对局中刷新页面的人永远进不来。

- [ ] 把 `me = room.seats.find(uid)` 提前：已在座直接返回当前状态

### 1.4 🔴 房间永不清理 —— `lastActive` 只写不读

建房后没人进就关页面 → 房间永久残留，内存只增不减。

- [ ] `setInterval` 每 5 分钟扫一次，删 `lastActive` 超过 60min 的房
- [ ] `MAX_ROOMS` 上限（如 200），满了拒绝创建

### 1.5 🟡 健壮性小项

- [ ] `handleCreate` 数值校验：盲注/初始瓜子必须是有限正数（负数/NaN 会把房间搞坏）；`body.me` 兜底默认值
- [ ] `roundNo` 自增：第二局起 `room.roundNo++`（现在永远显示第 1 局）
- [ ] 请求体大小上限 ~16KB
- [ ] 洗牌换 crypto：`createShuffledDeck(gameType, () => crypto.randomInt(2**32) / 2**32)`
- [ ] 5xx 加 `console.error`，不然云托管排障没日志

### 1.6 🟡 决策项：下一手筹码策略

服务端第二局沿用残余筹码（输光的人 seeds=0 永远陪坐）；本地版每手重置。
建议：每手 `start` 时把在座成员重置为 `initialSeeds`（和线下体验一致）。

---

## 2. 线下模式（PG RPC + 前端调用）

### 2.1 🔴 结算必然弹「结算失败」—— 函数名笔误

`OfflineRoomView.vue` `settleAndRestart` 里调用 `refreshSettlement()`，实际函数名是 `refreshSettlementPreview`（定义在 ~772 行）。ReferenceError 被 catch → 弹「结算失败」toast，且 `pullSnapshot`/`syncMyGoldenSeeds` 被跳过 → 弹窗不消、用户反复点、每次都报错（服务端 `offline_settle` 其实已成功，幂等保护才没重复转账）。

- [ ] `refreshSettlement()` → `refreshSettlementPreview()`

### 2.2 🔴 金瓜子重复记账（两套账本并存）

`settleAndRestart`/`settleAndExit` 先 `payGoldenSeeds()`（→ `transfer_seeds` RPC → 写旧表 `users.golden_seeds`/`seed_ledger`），再 `offlineSettle`（→ `account_seed_transfer` → 写新表 `accounts`/`account_ledger`）。一次结算写两套账。

- [ ] 删掉 `payGoldenSeeds` 及其在 794/810 行的调用 —— `offline_settle` 已在服务端记账
- [ ] 连带修 `user.js:205-241 recordSeed`：云端分支走 `account_seed_transfer`（账号用户），不要再调 `transfer_seeds`（旧 uid 账本，写了读不回）
- [ ] 连带修 `clearLedgerRow`（`user.js:264`+`ProfileView.vue:60`）：账号用户的账本行 key 是**账号名**，应调 `repo.clearAccountLedgerRow(peer)`，现在调的 `clear_seed_ledger_entry` 是旧表、uid 对不上账号名 → 清账按钮实际无效

### 2.3 🟡 `users.upsert` 每次建房/加入都清零 —— `cloud-repo.js:269, 316`

`upsert({golden_seeds: 0, total_games: 0})` 冲突时覆盖全部列 → users 行被重置。
改为 `Prefer: resolution=ignore-duplicates` 或只传 `id/nickname/avatar`。

### 2.4 🟡 `local-repo.js` 混入云端代码 —— `local-repo.js:255-260`

`listJoinableRooms` 里调 `rdb()`（未导入 → ReferenceError）且引用不存在的 `limit` 变量。本地模式点「加入」页签必炸。
改为读本地 `K.rooms` 或直接返回 `[]`。

### 2.5 🔴 SQL 函数版本漂移 —— 没有单一事实来源

同一函数被多个文件重复定义，**库里跑的是"最后执行的那条"**，文件时间线决定一切：

| 函数 | 定义次数 | 最新版（应保留） |
|---|---|---|
| `room_snapshot` | **6 处** | `fix-settlement-flow.sql`（含 autoBlind + roundStatus + guest + log50）|
| `move_pot` | 5 处 | `fix-settle-trigger.sql`（收款人归零兜底）|
| `offline_settle` | 5 处 | `fix-settle-winner-server-side.sql`（服务端裁定赢家）|
| `rotate_blinds` | 3 处 | `_rotate_blinds_patch.sql`（任何成员可触发）|
| `set_blinds` | 3 处 | `pot-log-sync.sql` / `_set_blinds_only.sql`（内容一致）|
| `is_room_member` / `register_account` / `account_available` | 各 2 处 | `fix-rls-recursion.sql` / `_accounts_regex_fix.sql` |

风险：任何人重跑 `offline-functions.sql` 或 `schema.sql` 都会**回退函数版本**，前端 `p_guest` 参数直接报错、settling/autoBlind 字段消失。

- [ ] 从线上库导出当前真实定义（`pg_get_functiondef`）固化成 `db/current-live.sql`（或按依赖顺序整理 `schema-v2.sql`），作为唯一事实来源
- [ ] `fix-*.sql`、`_*.sql` 补丁全部归档到 `db/archive/`
- [ ] `reorder_seats` 只存在于 `blind-rotation.sql`，前端在用 —— 合并时必须带上

### 2.6 🔴 `pot-log-sync.sql` 语法错误 —— 第 150 行

`END IF` 缺分号，整文件执行报错（库里实际生效的是 `_rotate_blinds_patch.sql` 版本，行为一致所以没出事）。修文件防误跑。

### 2.7 🟡 `fix-seed-accounting.sql`（v1）是已知失败版

文件自述失败原因（视图依赖）。归档或删除，避免误执行。

### 2.8 🟡 `list_joinable_rooms` 排序字段写错 —— `list-joinable-rooms.sql:35`

`jsonb_agg(t ORDER BY t.updated_at)`：`t` 是 jsonb 值，`.` 字段访问语法不成立（jsonb 不是 composite）→ 创建/调用报错。
改为 `ORDER BY sub.updated_at`（子查询第二列已为此预留）。**这是最新文件（今天 09:19），大概率未实测，先验证**。

### 2.9 🟡 `total_games` 永不持久化

`settleAndExit` 里 `update({totalGames:+1})` 只写本地对象，`update_my_account` RPC 不收这个字段 → 刷新即丢。
二选一：`offline_settle` 里 `UPDATE accounts SET total_games = total_games + 1`（推荐），或删掉这个字段的展示。

### 2.10 🟡 `leave_online_room` 无身份校验 —— `schema.sql:359`

任何成员调用都删整个房间。线上模式转容器后这个 RPC 应弃用；若前端还有入口会调到它，补 `host_uid = auth.uid()` 校验。

### 2.11 🟢 边缘：座位空洞导致小麦落空

中间座位退出后 `seat_no` 有空洞，`rotate_blinds` 里 `v_new_sb = 旧大麦座位` 可能指向已退出的座位 → 小麦没人下注。`reorder_seats` 后自愈，低概率，先记着。

### 2.12 🟢 决策项：`history` 只有创建者可见

`history_select_own` 限 `created_by = auth.uid()` → 只有房主能看到自己记的历史。朋友局可能想全员可见，决定一下。

---

## 3. 部署配置（云托管侧）

- [ ] **实例数固定为 1**：内存态服务，多实例 = 房间随机"不存在"
- [ ] 健康检查路径配 `/health`（`/` 返回 404）；Dockerfile 可加 `HEALTHCHECK CMD wget -qO- http://127.0.0.1:${PORT}/health || exit 1`
- [ ] 容器规格用最低档即可（零依赖纯内存）
- [ ] 前端 `VITE_DATA_SOURCE=cloud` + `VITE_ONLINE_API_BASE` 打进构建
- [ ] PG 侧线上模式遗留（`hands`/`decks`/`leave_online_room`/`cleanup_stale_rooms`）随容器上线自然弃用，不用动

---

## 4. 首次 `git init` 前仓库卫生

- [ ] `.gitignore` 补 `dist.zip`、`test-out.txt`；注释重写为 UTF-8（现是 GBK 乱码）
- [ ] 删根目录过期 `package-lock.json`（还是 `hamster-poker`+`concurrently`，与现 `package.json` 不符，`npm ci` 会挂）或 `npm install --package-lock-only` 重生成
- [ ] `.env.example` 加 `VITE_ONLINE_API_BASE=`
- [ ] 删 `cloud-functions/` 里 `hello`、`pg-probe`、`pg-probe2` 探针（`whoami` 可留作联调）
- [ ] 删 `/testroom` 路由 + `TestRoomView.vue`（会进生产包）
- [ ] `db/wipe-all-data.sql`、`cleanup-test-data.sql` 危险脚本确认不进任何自动流程
- [ ] `docs/线上模式任务拆分.md` 标注架构已变更为云托管容器，避免按旧方案施工
- [ ] `client/.env` 已在 ignore 中 ✓，确认首次提交时没被带上去

---

## 5. 建议执行顺序

1. **2.1**（一行笔误，但每次结算必炸）
2. **1.2 → 1.3 → 1.4**（服务端一个文件，半小时）
3. **2.2 → 2.3 → 2.4**（账本链路和本地模式修复）
4. **1.1**（前端接线，工作量主体）
5. **2.8**（验证新函数）→ 2.5 归档合并 → 其余 🟡/🟢 顺手
6. **第 3、4 节**部署与提交前一次做完

---

## 不做清单（已评估，明确跳过）

| 项 | 原因 |
|---|---|
| `parseUid` 不验签 JWT | 朋友局，uid 能区分人即可 |
| CORS 反射任意 Origin | 无敏感数据 |
| `handleState` 无座位校验 | 反而方便围观和断线重连 |
| `accounts`/`users` 可自改金瓜子、`account_seed_transfer` 任意转账 | 数字无意义，兄弟不防 |
| `login_account` 知道账号名即登录 | 产品设定如此 |
| 请求限流/HTTPS 头 | 云托管入口兜底 |
