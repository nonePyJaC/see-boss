# 仓鼠聚会 — 开发进度

> 最后更新：2026-09-20
> 状态：**游戏逻辑 + 前端闭环已在浏览器实测跑通**；数据层待 PostgreSQL 实测后接入

---

## 1. 已完成并验证

### 1.1 纯逻辑层（`shared/logic/`）— 69/69 测试通过

| 文件 | 职责 | 测试数 |
|---|---|---|
| [cards.mjs](../shared/logic/cards.mjs) | 牌模型、长牌 52 / 短牌 36、Fisher-Yates 洗牌 | — |
| [hand-evaluator.mjs](../shared/logic/hand-evaluator.mjs) | 牌型判定，**短牌同花 > 葫芦**，A 可当 5 也可当 14 | 13 |
| [side-pot.mjs](../shared/logic/side-pot.mjs) | 边池分层（职业比赛规则）、分配、余数守恒 | 10 |
| [betting.mjs](../shared/logic/betting.mjs) | 下注状态机、四轮下注、自动翻牌、行动流水、动态加注档位 | 34 |
| [settlement.mjs](../shared/logic/settlement.mjs) | 归零检测、平局加赛、金瓜子双向账本 | 12 |
| [dev-server.mjs](../shared/logic/dev-server.mjs) | 本地 HTTP 入口（8787），后续迁云函数的接口蓝本 | — |

**关键正确性保证**：
- 筹码守恒：任意操作序列后 `pot + Σseeds === 初始总和`（含多人全下场景）
- 边池守恒：各层金额之和 === 总投入
- 账实相符：`goldenSeeds === Σ ledger.count`，清账后仍成立
- 摊牌守恒：赢家分得之和 === 奖池

### 1.2 素材（`shared/assets/`）— 程序化 SVG

- **6 个仓鼠头像**：5 维度参数化（毛色/耳朵/配饰/眼睛/斑块），风格统一
- **瓜子筹码**：8 种面值配色（1/5/10/20/50/100/500/1000）
- **扑克牌**：牌面 + 瓜子纹牌背
- **金瓜子**：结算特效用
- **SeedChips 组件**：把任意数字渲染成一叠瓜子筹码（如 3480 → 500×6 + 100×4 + 50×1 + 20×1）

### 1.3 前端（`client/`）— 浏览器实测通过

| 页面 | 状态 |
|---|---|
| 注册页 | ✅ 昵称校验 2-8 字、6 头像选择、localStorage 自动登录 |
| 大厅 | ✅ 两种模式入口、金瓜子徽标 |
| 个人中心 | ✅ 账本每仓鼠一行、**长按清空（双方同时清）**、全部数据清空 |
| 线下计分 | ✅ 创建/加入、±1 瓜子、快捷档位 5/10/20/50/100/500/自定义、盲注标记、局数、归零结算弹窗 |
| 线上对局 | ✅ **完整一局实测跑通** |

线上对局实测（3 人局，长牌，3000 初始，10/20 盲注）：
```
开始 → 跟注 20 → [自动翻 3 张] → 加注到 70 → [其他人都跟注]
→ 过牌 → [自动翻第 4 张] → 过牌 → [自动翻第 5 张] → 过牌
→ 摊牌：每人显示牌型名 + 构成牌型的 5 张牌 + 赢得的瓜子
```

---

## 2. 修掉的真 bug

| bug | 现象 | 原因 |
|---|---|---|
| `structuredClone` 拒绝 Vue 代理 | 点击跟注无反应，控制台 `DataCloneError` | 前端把 state 放 `ref()` 传给 `applyAction`，内部 `structuredClone` 无法克隆 Proxy。已在入口做普通化 |
| 全员归零误触发加赛 | 2 人局双方都归零时进入加赛分支 | `findTopPlayers` 没排除归零者，把并列的 0 当成"并列最多" |
| 全员弃牌时摊牌崩溃 | `比牌至少需要 5 张牌，收到 2 张` | 其他人全弃牌时没有公共牌，剩下的人只有 2 张底牌。已让 `showdown` 牌不足时跳过比牌，`distributePot` 单资格层直接通吃 |
| `bet` / `raise` 记录错乱 | 行动流水永远显示 raise | 判断时 `next.currentBet` 已被改成目标值，恒真。已捕获修改前的注额 |

---

## 3. 本地运行

```bash
npm install
npm install --prefix client
npm run dev          # 前端 5173 + 逻辑层 8787
npm test             # 69 个逻辑测试
npm run build        # 产出 client/dist（gzip 约 62KB）
```

手机同 WiFi 下访问 `http://<你的IP>:5173` 可看竖屏全屏效果。

---

## 4. 待办（按优先级）

### 4.1 数据层接入（阻塞上线）
需要先完成 4 项实测，见 [PostgreSQL架构评估.md](PostgreSQL架构评估.md) §4：

1. **环境地域**（上海 342 点/核·时 vs 新加坡 587 点/核·时）
2. **云函数能否连通 PostgreSQL**（免费版云函数不支持 VPC）
3. **Broadcast 能否从服务端发**（决定实时方案）
4. **PG CDC 是否真可用**（官方文档自相矛盾）

接入点已预留：
- [dev-server.mjs](../shared/logic/dev-server.mjs) 的 4 个端点即云函数签名
- `stores/user.js` 的 `recordSeed` / `clearLedgerRow` 已把"本地先改、云端同步"分离
- 表结构与 RLS 完整 SQL 已写好（[PostgreSQL架构评估.md](PostgreSQL架构评估.md) §2）

### 4.2 功能补全
- [ ] 二维码（`qrcode` 依赖已装，页面还是占位）
- [ ] 音效（Web Audio 程序合成，零授权风险）
- [ ] 加赛 UI（线上 1V1 / 线下房主点选）— 逻辑已在 `settlement.mjs`，界面未做
- [ ] 断线重连恢复牌局
- [ ] 金瓜子获得特效（GSAP）
- [ ] 行动流水改为只显示当前阶段（避免刷屏后看不到关键加注）

---

## 5. 文件结构

```
D:\newPoker\
├── package.json                  # 根脚本，npm run dev 同时起前后端
├── client/                       # Vue3 + Vite 前端
│   ├── index.html                # viewport-fit=cover，dvh 全屏
│   ├── vite.config.js            # hash 路由、@shared 别名
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── styles/main.css       # 暖色卡通设计系统
│       ├── components/
│       │   └── SeedChips.vue     # 瓜子筹码可视化
│       ├── stores/user.js        # 用户档案 + 金瓜子账本（本地优先）
│       └── views/
│           ├── RegisterView.vue
│           ├── LobbyView.vue
│           ├── ProfileView.vue
│           ├── OfflineRoomView.vue
│           └── OnlineRoomView.vue
└── shared/
    ├── logic/                    # 纯函数游戏逻辑（前后端共用）
    │   ├── cards.mjs
    │   ├── hand-evaluator.mjs
    │   ├── side-pot.mjs
    │   ├── betting.mjs
    │   ├── settlement.mjs
    │   ├── dev-server.mjs
    │   └── *.test.mjs            # 69 个测试
    └── assets/                   # 程序化 SVG 素材
        ├── hamsters.mjs
        └── visuals.mjs
```
