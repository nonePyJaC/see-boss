# 数据库设计与安全规则（最终定稿）

> 2026-09-20 ｜ 环境 ID：`see-boss-d2gjggfbz8808d1d4`（上海 / 云数据库）
> 本文件包含可直接复制到控制台的集合配置与安全规则 JSON

---

## 0. 核心原则

**客户端只读，所有写操作走云函数。**

云函数使用服务端 SDK（管理员身份）访问数据库，**不受下面的安全规则限制**。安全规则只约束"浏览器直连数据库"的权限——这是防作弊的关键：玩家无法直接改自己的筹码、无法直接改金瓜子。

---

## 1. 为什么是 4 个集合，不是 3 个

原计划 3 个（users / rooms / hands），但漏了一个：**洗好的牌堆不能放 rooms**。

如果把整副洗好的牌存进 `rooms`，任何人 watch 房间就能看到后续所有公共牌和发牌顺序 → 直接作弊。所以牌堆必须放在一个**客户端完全不可读**的集合里。

| 集合 | 谁可读 | 作用 |
|---|---|---|
| `users` | 仅本人 | 昵称、头像、金瓜子、账本 |
| `rooms` | 仅房间成员 | 公共状态（筹码、奖池、公共牌、轮到谁） |
| `hands` | 仅本人 | 自己的底牌 |
| `decks` | **无人（仅云函数）** | 洗好的牌堆、发牌进度 |

---

## 2. 集合与安全规则（控制台直接复制）

### 2.1 `users` — 用户档案 + 金瓜子账本

```json
{
  "read": "doc._id == auth.uid",
  "create": false,
  "update": false,
  "delete": false
}
```

> Web 端身份标识是 `auth.uid`（不是 `auth.openid`，那是小程序端的）。
> 不写 `write` 键 = 默认 false（禁止写入）。

**文档结构**
```js
{
  _id: "<uid>",                       // 匿名登录的 uid
  nickname: "仓鼠王",
  avatar: 3,                          // 1-6，对应 6 个仓鼠头像
  goldenSeeds: 5,                     // 金瓜子总数，可为负
  seedLedger: [                       // 每个仓鼠一行
    { uid: "u2", nickname: "小明", avatar: 1, count: 3 },   // 他给了你 3 颗
    { uid: "u3", nickname: "阿花", avatar: 4, count: -2 }   // 你给了他 2 颗
  ],
  totalGames: 12,                     // 参与局数（可选）
  createdAt: 1726...,
  updatedAt: 1726...
}
```

**账本规则**
- `count > 0`：该仓鼠给了你 `count` 颗
- `count < 0`：你给了该仓鼠 `|count|` 颗
- `goldenSeeds` = 所有 `count` 之和（每次写入时重算，保证账实相符）
- `count` 归零的条目直接删除，不留空行

---

### 2.2 `rooms` — 房间公共状态

```json
{
  "read": "auth.uid in doc.members",
  "create": false,
  "update": false,
  "delete": false
}
```

> `auth.uid in doc.members` 是腾讯云官方文档给出的房间类集合标准写法（见[数据库安全规则](https://docs.cloudbase.net/database/security-rules)的群聊示例）。
> 客户端用 `db.collection('rooms').doc(roomId).watch()` 直接按文档 ID 订阅，不需要 `where()` 查询，因此不触发"查询条件必须是安全规则子集"的限制。
> ⚠️ 若实测该规则不生效，备用方案见 §6。

**文档结构**
```js
{
  _id: "482913",                      // 6 位房间号
  mode: "online",                     // online=线上发牌 | offline=线下计分
  gameType: "long",                   // long=长牌 | short=短牌（仅线上）
  status: "waiting",                  // waiting|playing|settling|finished
  hostUid: "<uid>",
  initialSeeds: 3000,
  smallBlind: 10,
  bigBlind: 20,
  round: 1,                           // 第几局
  dealerUid: "<uid>",                 // 庄家位（按钮位）
  members: ["uid1", "uid2", "uid3"],  // 扁平 uid 数组，供安全规则判断

  seats: [                            // 座位，按入座顺序
    {
      uid: "uid1", nickname: "仓鼠王", avatar: 3,
      seeds: 3000,                    // 剩余瓜子
      bet: 0,                         // 本轮已下注
      totalBet: 0,                    // 本局累计投入（算边池用）
      folded: false,
      allIn: false,
      isTurn: false                   // 是否轮到该玩家行动
    }
  ],

  // ── 以下仅线上模式使用 ──
  pot: 0,
  sidePots: [],                       // 边池，见 §4
  communityCards: [],                 // 公共牌
  phase: "idle",                      // idle|preflop|flop|turn|river|showdown
  currentBet: 0,                      // 当前轮最大下注额
  minRaise: 20,                       // 最小加注额
  turnUid: "<uid>",
  lastAggressorUid: "<uid>",          // 最后一个加注者，用于判定下注轮结束
  actedUids: [],                      // 本轮已行动过的 uid

  // ── 结算 ──
  settlement: null,
  // 有人归零时变为：
  // { triggeredBy: "uid3", toUid: "uid1", at: 1726..., hostUid: "uid1" }

  createdAt: 1726...,
  updatedAt: 1726...
}
```

**`members` 与 `seats[].uid` 是重复的**，这是故意的：`members` 是扁平数组，专供安全规则判断，`seats` 带完整状态。

---

### 2.3 `hands` — 底牌（每人只能看自己的）

```json
{
  "read": "doc.uid == auth.uid",
  "create": false,
  "update": false,
  "delete": false
}
```

**文档结构**
```js
{
  _id: "482913_uid1",                 // {roomId}_{uid}
  roomId: "482913",
  uid: "uid1",
  cards: ["As", "Kd"],                // A=ace, s/d/h/c=花色
  updatedAt: 1726...
}
```

**为什么必须单独放**：如果底牌塞进 `rooms`，所有人 watch 一次就全看到了。单独集合 + 仅本人可读，才能保证隐私。

---

### 2.4 `decks` — 牌堆（客户端完全不可见）

```json
{
  "read": false,
  "create": false,
  "update": false,
  "delete": false
}
```

> 四个操作全部关闭 = 保持默认 false。这个集合**只有云函数能读写**。

**文档结构**
```js
{
  _id: "482913",
  deck: ["2c", "7d", "As", ...],      // 洗好的整副，按发牌顺序排列
  dealt: 2,                           // 已发到第几张（索引）
  burned: [],                         // 翻牌前烧掉的牌
  updatedAt: 1726...
}
```

---

## 3. 金瓜子账本：转账与清空

### 3.1 转账（结算时自动执行）

有人瓜子归零 → 给当前瓜子最多的人 1 粒金瓜子：

```
users/A: seedLedger 中 B 的 count -= 1;  goldenSeeds -= 1
users/B: seedLedger 中 A 的 count += 1;  goldenSeeds += 1
```

两个文档必须在**同一个事务**里改，否则会出现"我账上有、他账上没有"的坏账。

### 3.2 清空某一行（长按统计行）

用户长按「小明 给了你 3 颗」→ 双方同时清零：

```
users/我: 删除 seedLedger 中小明的条目;  goldenSeeds -= 3
users/小明: 删除 seedLedger 中我的条目;  goldenSeeds += 3
```

**这一步必须走云函数**：它要写另一个用户的文档，而客户端安全规则禁止你写别人的 `users`。这也正是"给予方和收获方都清空避免坏账"的实现方式。

### 3.3 为什么金瓜子可以是负的

归零的人自己可能一颗金瓜子都没有，转账后就是 `-1`。这是符合你要求的正常状态，不做钳位（clamp）到 0。

---

## 4. 结算流程（按你定的规则）

```
status: playing
   │
   │ 任意玩家 seeds === 0（云函数检测到）
   ▼
status: settling
   │  rooms.settlement = { triggeredBy, toUid, at, hostUid }
   │  所有玩家手机同时看到"本局结束"（watch 推送）
   ▼
房主手机弹出结算框 ────┬──── 选「结算重开」          选「结算退出」 ────┐
                       │                                    │
                       ▼                                    ▼
              ① 转账 1 粒金瓜子                        ① 转账 1 粒金瓜子
                 triggeredBy → toUid                      triggeredBy → toUid
              ② 全员 seeds = initialSeeds              ② status = finished
              ③ round += 1                            ③ 所有客户端收到 finished
              ④ 清空 board / hands / decks               → 自动返回首页
              ⑤ status = waiting
                 → 重新开始
```

**两个选项都会先结算金瓜子**，区别只在是否重开。

### 边界情况的默认处理（已定稿）

| 情况 | 处理 |
|---|---|
| **多人同时归零** | 每个归零者各给瓜子最多者 1 粒（5 人局 2 人归零 → 唯一最多者 **+2 粒**） |
| **并列瓜子最多** | 见下方「平局加赛」 |
| **归零者就是瓜子最多的人** | 仅全员皆 0 时可能出现，此时跳过转账 |
| **房主掉线/关页面** | 房间停在 `settling`，**90 秒后自动执行「结算重开」**，避免房间卡死 |
| **房主本人归零** | 房主照样弹框选择，规则不变 |

### 平局加赛（并列瓜子最多）

```
有人归零
   │
   ▼
maxSeeds = max(seats[].seeds)
tiePlayers = seats.filter(s => s.seeds === maxSeeds && 未归零)
   │
   ├─ tiePlayers.length === 1 ──► 正常结算：每个归零者给唯一最多者 1 粒
   │
   └─ tiePlayers.length > 1 ─────► status = playoff（加赛）
                                      │
                    ┌─────────────────┴─────────────────┐
                    ▼                                   ▼
            线上模式                            线下模式
        并列者打 1V1                       房主弹框：
        谁赢谁收全部金瓜子                  点击谁是最后赢家
```

**线上加赛的实现**：`rooms.status = "playoff"`，`playoff = { mode: "online", participants: [uid1, uid2] }`。用同一套发牌/比牌引擎，只给这两人发底牌 + 一副完整公共牌，**无下注轮**（金瓜子已由归零者付出，加赛只决定归属），直接摊牌比大小。

**线下加赛的实现**：`rooms.status = "playoff"`，`playoff = { mode: "offline", participants: [...] }`。房主端弹框列出并列者头像，房主点击某人 → 该 uid 获得全部金瓜子。

**加赛结束后的流程一致**：转账金瓜子 → 房主弹「结算重开 / 结算退出」→ 按主结算流程走。

> ⚠️ 线上加赛我默认**无下注轮**（直接发牌比大小）——金瓜子已由归零者付出，加赛无筹码赌注，走下注流程没有意义且拖长时长。若你希望加赛也走完整下注，告诉我。

---

## 5. 短牌规则（最终定稿）

### 牌堆
去掉 2/3/4/5 → **36 张**：A、6、7、8、9、T、J、Q、K × 4 花色

### 牌型大小（从大到小）

| # | 牌型 | 备注 |
|---|---|---|
| 1 | 皇家同花顺 | T-J-Q-K-A 同花 |
| 2 | 同花顺 | |
| 3 | 四条 | |
| 4 | **同花** | ⚠️ **唯一**与标准长牌不同的地方：同花 > 葫芦 |
| 5 | 葫芦 | |
| 6 | 三条 | |
| 7 | 顺子 | 最低 6-7-8-9-T，最高 T-J-Q-K-A |
| 8 | 两对 | |
| 9 | 一对 | |
| 10 | 高牌 | |

> 完整顺序：高牌 < 一对 < 两对 < 三条 < 顺子 < 葫芦 < 同花 < 四条 < 同花顺 < 皇家同花顺
> 短牌**不采用**职业 Short Deck 的「三条 > 顺子」和「三同花 > 同花顺」规则。

### A 的双重用法
- 作 14：T-J-Q-K-A（最大顺子）
- 作 5：A-6-7-8-9（最小顺子）

### 盲注
大小盲注仍在房间内设置，与长牌一致。

---

## 6. 备用方案（如果 `rooms` 的成员校验规则不生效）

如果实测 `auth.uid in doc.members` 无法通过 watch 生效，改为**每玩家投影**方案：

- 新增集合 `roomViews/{roomId}_{uid}`
- 安全规则：`{"read": "doc.uid == auth.uid"}`
- 云函数每次改房间状态时，为每个成员写一份自己的投影文档
- 客户端 watch 自己的投影，而不是 `rooms`
- `rooms` 改为客户端完全不可读

代价：每次状态变更多写 N 份文档（5 人 = 5 次写）。按一局 ~150 次状态变更算，750 次写 ≈ 0.015 元，可忽略。

**好处**：投影文档可以只包含该玩家有权看到的信息，隐私性更强。

---

## 7. 费用重估（含账本开销）

| 项目 | 单价 | 月用量 | 资源点 |
|---|---|---|---|
| 静态托管流量 | 210 点/GB | 25MB | ~5 |
| 数据库调用 | 200 点/万次 | ~20,000 | ~400 |
| 云函数调用 | 13.3 点/万次 | ~20,000 | ~27 |
| 云函数资源 | 0.11108 点/GBs | ~4,000 | ~444 |
| 身份认证 | 500 点/万次 | 20 | ~0.1 |
| **合计** | | | **≈ 880 点/月** |

免费额度 3000 点/月，占用不到 1/3。

> ⚠️ 仍未核实：`watch()` 长连接是否按变更次数额外计费。上线第一局后去「套餐用量页」核对实际消耗。

---

## 8. 控制台操作清单

1. **文档型数据库** → 新建 4 个集合：`users` / `rooms` / `hands` / `decks`
2. 每个集合 → 「权限管理」→「切换到安全规则」→ 粘贴 §2 对应 JSON
3. **身份认证 → 登录方式** → 开启「匿名登录」
4. **安全来源** → 确认 `*.tcloudbaseapp.com` 已在白名单（文档说默认已含）
5. 记下环境 ID：`see-boss-d2gjggfbz8808d1d4`

---

## 9. 一句话

**4 个集合，客户端只读；牌堆单独关进 `decks` 防作弊；金瓜子账本走云函数做双向清账；`rooms` 用成员数组做读权限。§2 的 JSON 可以直接粘进控制台。**
