# 《仓鼠聚会》H5 网页游戏 — 可行性分析与部署评估

> 分析日期：2026-09-20
> 状态：**仅分析，未写任何功能代码**
> 分析对象：[仓鼠聚会.md](仓鼠聚会.md)

---

## 0. 结论摘要（先看这里）

| 维度 | 结论 |
|---|---|
| 整体可行性 | ✅ **完全可行**。技术栈（Vue3+Vite+Express+Socket.io）成熟，需求无技术禁区 |
| 最大技术风险 | ⚠️ **CloudBase 网关 60 秒空闲回收连接** + **内存存储在云托管多实例下会丢房间数据**。需要针对性设计（心跳、单实例、Redis） |
| 最大需求缺口 | ❗**"内存存储"与"金瓜子永久累计"直接矛盾**。服务器重启即清零。必须改用 localStorage 或云数据库，需要你决策 |
| CloudBase 能否自主部署 | ✅ **能**。官方明确支持 WebSocket 容器型部署，Dockerfile / 代码包 / Git / CLI 四种方式，端口自由，无需 Nginx 反代 |
| 成本 | 云托管**不是免费的**。常驻 1 个最小实例约 **22 元/月** + 出流量 0.8 元/GB；静态托管仅付流量 0.21 元/GB |
| 上线硬门槛 | 🔴 **ICP 备案**（大陆正式访问必须）+ **至少个人版套餐 19.9 元/月**（免费体验版绑不了自定义域名，且云托管功能受限） |
| 需求里的 Vercel 部分 | ❌ 与"部署在 CloudBase"冲突，且国内访问有合规/稳定性问题。建议**降级为可选附加项**，不作为主线 |

---

## 1. 需求拆解与逐项可行性

### 1.1 前端：Vue3 + Vite + Socket.io-client + GSAP + Vant 4

| 需求项 | 可行性 | 说明 |
|---|---|---|
| Vue3 + Vite | ✅ 无风险 | 标准组合，CloudBase 静态托管有官方 Vue SPA 部署教程 |
| Socket.io-client | ✅ 可行，⚠️ 有细节 | 见 §2.2。云托管官方 WebSocket 示例用的是 `express-ws`，**没有 Socket.io 官方示例**，属"协议层可行但未官方验证" |
| GSAP | ✅ 无风险 | 纯前端动画库，发牌掉落/翻牌翻转/弹跳都用它，注意移动端 GPU 合成（用 transform/opacity） |
| Vant 4 | ⚠️ **部分冲突，建议裁剪** | Vant 是表单/列表/弹窗类组件库，适合注册页和大厅。但需求同时要求"手机竖屏全屏、大按钮、卡通风、无多余广告元素"——Vant 的默认 Material/iOS 风格与游戏化视觉**气质不符**，且会引入约 100KB+ 体积。**建议：仅用于注册表单、数字键盘、Toast/ Dialog，牌桌/筹码/动画全部自绘** |
| qrcode | ✅ 无风险 | 用 `qrcode` 或 `qr-code-styling` 生成 dataURL，注意生成仓鼠造型二维码是加分项但非必需 |
| 移动端竖屏全屏 | ✅ 需处理 | `100dvh` + `viewport-fit=cover` + 安全区 `env(safe-area-inset-*)`；横屏给提示或强制竖屏 |

**PC 端策略**：需求写"禁止 PC 端冗余布局"。建议做法是 PC 上显示一个"请用手机扫码/打开"的引导页（含二维码），而不是把牌桌拉伸变形。这一点需要你确认（见 §6）。

### 1.2 后端：Node.js + Express + Socket.io，内存存储

| 需求项 | 可行性 | 说明 |
|---|---|---|
| Express + Socket.io | ✅ 无风险 | 单进程即可支撑几十人同时游戏 |
| 内存存储房间/用户 | ✅ 本地开发无风险 | ⚠️ 云端有两个问题：① 见 §2.1 持久化矛盾；② 见 §2.3 多实例丢数据 |
| 无数据库 | ✅ 可以 | 但要接受"重启即丢全部房间"的后果。对"朋友聚会当场开一局"场景其实可接受 |

### 1.3 业务功能可行性

**首次进入 / 注册 / 自动登录**
- ✅ 昵称 2-8 字校验 + 6 个 SVG 仓鼠头像，纯前端即可
- ⚠️ 没有账号体系，"自动登录"只能靠 `localStorage` 存一个 userId（建议 `crypto.randomUUID()`）。换设备/清缓存即换人
- ❗ 这里直接引出 §2.1 的核心矛盾

**线下计分模式（手机当筹码计分器）**
- ✅ 技术上最简单，本质是"共享计数器"，Socket.io 广播即可
- ⚠️ **需求描述有歧义，需要补齐规则**（见 §6）
  - "左边收瓜子 / 右边出瓜子"：每个玩家是操作**自己**的筹码，还是能操作任意玩家？"收"是谁给谁？
  - "按钮中间区域展示当前玩家状态 大盲注/小盲注/其他"：盲注位如何轮转？
  - 下注、弃牌在纯线下计分里是什么语义（不涉及牌局，只是记账？）
  - 多副实体牌同时？要不要记录"局数"和"当前盲注"的自动轮换规则？
- ✅ 长按快捷出瓜子（5/10/20/50/100/500/自定义）：用 pointerdown 计时 + 重复触发，注意长按后要阻止 click 事件双触

**线上游戏模式（德州扑克）**
- ✅ 长牌（52 张）实现路径清晰：洗牌 → 发底牌 → 盲注 → 翻牌前/翻牌/转牌/河牌 四轮下注 → 摊牌比大小
- ⚠️ **短牌（36 张，6-A）需要明确的规则约定**：默认建议
  - 牌堆去 2-5；A 可作 6 也可作 14（顺子 6-7-8-9-10 与 10-J-Q-K-A 均成立，A-6-7-8-9 有时也允许）
  - 牌型大小：**三同花 > 同花顺 > 顺子 > … > 高牌**（即三张同花色胜过同花顺）
  - 短牌常用 ante（前注）而非大小盲注——需求又要求"大小盲注"，需二选一
- ⚠️ **边池（Side Pot）算法**是全下玩法最复杂的部分。需求只写"全下"，没提边池。多人多次全下必须实现边池，否则筹码会算错。这是实现里最容易出 bug 的模块
- ⚠️ **服务端权威**：牌、下注状态必须全在服务端，前端只渲染。否则用户改 JS 就能作弊
- ⚠️ **断线重连恢复牌局**：手机熄屏/切微信回来，必须能从服务端状态重建手牌。这块工作量常被低估

**结算规则（全局通用）**
- ✅ "瓜子归零 → 给第一名 +1 金瓜子 → 重置所有人瓜子 → 下一局"
- ⚠️ 需求细节待确认：
  - 线上模式中"归零"是指全下输光（正常），还是任意时刻归零（线下正常）？线上模式一个玩家输光后是**退出房间**还是**自动复活继续**？需求写"自动重置所有玩家"，即全员复活
  - 金瓜子是 +1，那线下模式和线上模式的 +1 是否等价？会不会导致刷分？

**视觉与交互**
- ✅ 全部可实现。卡通风、暖色调、瓜子筹码、GSAP 动画、金瓜子获得特效
- ⚠️ 工作量集中在美术（见 §4），SVG 头像和瓜子可程序化生成，成本可控

### 1.4 工程要求

| 需求 | 可行性 | 备注 |
|---|---|---|
| 根目录 package.json，`npm run dev` 同时起前后端 | ✅ 用 `concurrently` | 建议 `concurrently "npm:dev:server" "npm:dev:client"` |
| 核心文件注释 | ✅ | |
| Dockerfile + docker-compose | ✅ | CloudBase 云托管要求 Dockerfile 监听 `0.0.0.0` + `process.env.PORT`，与本地 docker-compose 兼容 |
| vercel.json + 后端适配 Vercel Functions | ❌ **技术上不成立** | Vercel Functions 是 Serverless，**不支持 WebSocket 长连接**，Socket.io 无法在 Vercel Functions 上跑。这条需求本身有技术错误，建议删除或改为"前端单独部署 Vercel、后端仍用 CloudBase" |
| 完整部署步骤文档 | ✅ | |

---

## 2. 关键技术风险（重点）

### 2.1 ❗「内存存储」与「金瓜子永久累计」直接矛盾（最高优先级）

需求同时要求：
- 后端"内存存储，无需数据库"
- 金瓜子"永久累计，记录在个人数据中"，且个人信息"永久保留"

**内存存储在进程重启/重新部署/多实例扩缩容后全部丢失**，不可能"永久累计"。这是需求内部冲突，必须二选一：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 纯前端持久化（推荐）** | 金瓜子存 `localStorage`，服务端只管房间内实时瓜子 | 换设备/清缓存即丢失；无法跨设备；零后端成本 |
| **B. 加云数据库** | CloudBase 文档型数据库（Mongo 兼容）或云函数 + 数据库，按 openid 存 | 引入后端依赖，违背"无需数据库"初衷；调用计费 200 点/万次；需接入登录体系才有稳定身份 |
| **C. 服务端 JSON 落盘** | 云托管容器内写文件 | ⚠️ **不可行**：云托管实例无状态、自动扩缩容、重新部署即重置，文件不持久 |

**我的建议：方案 A**（localStorage），并在 UI 上明确告知"数据保存在本机"。原因：需求核心场景是"线下聚会当场开一局"，跨设备累计金瓜子不是刚需，且引入账号体系会让项目复杂度翻倍。

### 2.2 CloudBase 网关 60 秒空闲回收连接（硬约束）

腾讯云官方文档明确：云托管 HTTP 网关会**主动关闭长时间（60s）无数据传输的非活跃连接**。

对 Socket.io 的影响与对策：

| 问题 | 对策 |
|---|---|
| 心跳间隔必须 < 60s | Socket.io 默认 `pingInterval: 25s` / `pingTimeout: 20s`，**默认值安全，不要改大**。若用原生 `ws`，需自建 10-20s 心跳 |
| 客户端断线重连 | Socket.io 自带自动重连，但要把 `reconnectionDelay` 调小，并在 UI 上显示"重连中"状态条 |
| 大厅/等待页空闲 | 房间内无操作时也要有心跳保活；Socket.io 心跳包即可满足 |
| HTTP long-polling 降级 | 受 60s 请求超时限制，可能异常。**建议强制 WebSocket**（`transports: ['websocket']`），失败则提示用户而不是静默降级 |

### 2.3 云托管多实例会丢房间数据（重要）

CloudBase 云托管是**无状态多实例 + 自动扩缩容**（闲置 10 分钟回收，每环境最多 28 实例）。Socket.io 的 `rooms` / `broadcast` / `io.emit` 都是**进程内状态**：

- 玩家 A 连到实例 1，玩家 B 连到实例 2 → **互相收不到消息，房间直接失效**
- 这是 Socket.io 上云托管最实际的坑

三种对策：

| 方案 | 做法 | 评价 |
|---|---|---|
| **1. 单实例持续运行（推荐本项目）** | 部署时把实例数设为"持续运行，最小 1 最大 1" | ✅ 最省事，房间内广播天然可用；聚会游戏并发极低（<50 人），单实例足够；代价是无法弹性扩缩 |
| 2. Redis Adapter | `@socket.io/redis-adapter` + 外接 Redis（CloudBase 无内置 Redis，需另购） | ❌ 违背"零依赖"，额外成本和运维 |
| 3. 会话亲和性 | 云托管支持 `X-Cloudbase-Session-Id`（默认 4h） | ⚠️ 只保证同一客户端落同实例，**不解决两个不同客户端跨实例通信**，不能单独使用 |

### 2.4 免费体验版的硬限制

调研确认的 CloudBase 套餐限制：

| 套餐 | 价格 | 自定义域名 | 云函数超时 | 云托管 |
|---|---|---|---|---|
| 免费体验版 | 0 元 | **0 个** ❌ | 3s 且不可改 | 功能受限（无灰度/VPC/固定IP/IP黑白名单） |
| 个人版 | 19.9 元/月 | 1 个 ✅ | 最大 900s | 完整 |
| 标准版 | 199 元/月 | 5 个 | 最大 900s | 完整 |

**含义**：免费版**无法绑定自定义域名**，只能用默认域名（官方定位"仅限开发测试"，且有访问提示中间页）。所以：
- **开发/测试阶段 → 免费版够用**
- **正式上线 → 至少个人版 19.9 元/月**

另注意：免费版**不能开启"超限按量"**，超额即停服；云托管**必须开启超限按量**才能用。这两条对免费版用户是阻塞的，实际只能用于验证部署流程。

---

## 3. CloudBase 部署方案（能否自主部署：✅ 能）

### 3.1 推荐架构

```
┌─────────────────────────────────────────────┐
│  用户手机浏览器                              │
└───────────┬─────────────────────┬───────────┘
            │ HTTPS               │ WSS
            ▼                     ▼
┌────────────────────┐   ┌──────────────────────────┐
│ CloudBase 静态托管  │   │ CloudBase 云托管（容器型） │
│ Vue3+Vite 构建产物  │   │ Express + Socket.io      │
│ COS + CDN           │   │ 单实例持续运行            │
│ 0.21 元/GB 流量     │   │ CPU 0.055 + 内存 0.032    │
└────────────────────┘   └──────────────────────────┘
         │                          │
         └────────── 同一环境 ───────┘
```

### 3.2 部署步骤（自主部署完整路径）

**Step 1 — 前端部署到静态托管**
```bash
npm run build                 # 产出 client/dist
tcb hosting deploy client/dist -e <envId>
```
SPA history 路由需在控制台配置：**静态网站托管 → 设置 → 错误页面 → 4xx 错误页面填 `index.html`**（CloudBase 用"错误码重定向"实现 rewrite，不是 nginx 那种 rewrite 语法）。

**Step 2 — 后端部署到云托管**
```bash
tcb cloudrun deploy --port 3000 -e <envId>
```
关键约束：
- Dockerfile 必须 `EXPOSE` 真实端口，进程监听 `0.0.0.0`（监听 `127.0.0.1` 会部署失败）
- 端口用 `process.env.PORT || 3000`，部署时填同一个值
- 代码包部署要求仓库内含 Dockerfile；也可以推镜像到 TCR（个人实例免费）
- 在线构建超时 10 分钟，node_modules 建议在镜像内装（或用多阶段构建 + pnpm store 缓存）

**Step 3 — 配置项**
- 实例数：**持续运行 / 最小 1 / 最大 1**（对应 §2.3）
- 环境变量：`CLIENT_ORIGIN=https://你的域名`（CORS + Socket.io origin 白名单）
- 开启「超限按量」，否则云托管会被停服
- 健康检查：部署有 Readiness 探针，**不要求特定 `/healthz` 路径**，探针打配置端口（但仍建议加一个 `GET /healthz` 返回 200，便于自测）

**Step 4 — 域名与备案**
- 静态托管 + 云托管分别绑自定义域名，各需 HTTPS 证书
- 🔴 **自定义域名必须已完成 ICP 备案**，否则 403
- 好消息：2026-03-30 起**云开发环境本身可作为备案云资源**（需个人版及以上 + 资源剩余有效期 > 6 个月 + 固定公网 IP）
- 坏消息：**2021-08-05 起云托管不再支持新增域名备案**，需走"整个云开发环境备案"这条路
- 备案周期参考：个人约 7–20 天

### 3.3 成本估算（月）

| 项目 | 用量假设 | 费用 |
|---|---|---|
| 云托管 CPU | 0.25 核常驻 730h | 55点/核·h → 10,038 点 ≈ 10.0 元 |
| 云托管内存 | 0.5GiB 常驻 | 32点/GB·h → 11,680 点 ≈ 11.7 元 |
| 云托管出流量 | 10 GB | 800点/GB → 8,000 点 ≈ 8.0 元 |
| 静态托管流量 | 10 GB | 210点/GB → 2,100 点 ≈ 2.1 元 |
| 静态托管存储 | ~2 MB | 可忽略 |
| 云函数调用 | 若用 SDK 直连 | 100点/万次 ≈ 0.1 元/万次 |
| **合计** | | **≈ 32 元/月**（个人版含 40,000 点/月，超出部分按量） |

> 若月活几百人、偶尔几局，实际可能低于此估算。若想省钱，见 §3.4。

### 3.4 备选方案对比

| | CloudBase 云托管 | 轻量应用服务器 + Docker | Vercel |
|---|---|---|---|
| WebSocket | ✅ 官方支持，有 60s 空闲/多实例限制 | ✅ 完全自由，无网关限制 | ❌ Functions 不支持长连接 |
| 国内延迟 | ✅ 上海地域 + 边缘加速 | ✅ 可选国内地域 | ⚠️ 大陆无优化节点 |
| ICP 备案 | 必须（云开发环境可作备案资源） | 必须（标准备案资源，需包年包月≥3月） | 必须 + 自建回源，麻烦 |
| 运维 | ✅ 零运维，自动扩缩容/回滚/日志/监控 | ❌ 自管系统、证书、扩容、安全 | ✅ 零运维 |
| 成本 | ≈22 元/月常驻实例 + 流量 0.8 元/GB | ≈35–50 元/月（2C2G 含 100GB+ 流量） | Hobby 免费 / Pro $20 |
| 适合度 | ⭐⭐⭐⭐ 符合需求 | ⭐⭐⭐⭐ 更简单可控、流量更划算 | ⭐ 不适合本项目 |

**结论**：按需求用 CloudBase 云托管完全可行且能自主部署；若后期流量费敏感，迁移到轻量服务器只需换一个 Docker 运行环境，代码不用改（这也是"前后端分离 + Docker"架构的好处）。

### 3.5 需求中 Vercel 部分的处理建议

原需求第 4 条「生成 vercel.json，后端适配 Vercel Functions」**技术上不成立**——Vercel Functions 是 Serverless，无法保持 Socket.io 长连接。

建议改为：
- 保留 `vercel.json`，但只用于**前端静态部署**（`outputDirectory: client/dist` + SPA rewrite）
- 后端明确写"因 WebSocket 长连接限制，不支持 Vercel Functions，请用 CloudBase 云托管 / Docker / 轻量服务器"
- 若坚持要 Vercel 全栈，唯一路子是后端改用 SSE 推送（HTTP 云函数默认支持 SSE，无需配置），但那是另一套架构，不建议

---

## 4. 素材清单

### 4.1 必须有的（阻塞开发）

| 素材 | 规格/数量 | 来源 | 备注 |
|---|---|---|---|
| 仓鼠头像 SVG | 6 个，128×128 viewBox，可换色 | **可程序生成** | 建议做参数化生成器：体型/耳朵/花纹/围巾/眼睛 5 个维度组合，保证风格统一且易扩展 |
| 瓜子（筹码）SVG | 1 个基础形 + 5 种面值配色 | **可程序生成** | 向日葵籽造型，带高光和描边；面值用颜色区分 |
| 扑克牌面 | 52 张（或 SVG 拼合）+ 牌背 | **可程序生成** | 用 SVG symbol + 文本渲染花色与点数，避免 52 张图片 |
| 主视觉 / Logo | 1 张 | 需设计 | 「仓鼠聚会」标题字 + 仓鼠捧瓜子形象，可用 SVG 绘制或 AI 生图 |
| 按钮/面板视觉 | 弹窗、按钮、玩家席位 | 可程序生成（CSS+SVG） | 圆角、暖色渐变、描边，符合"可爱卡通暖色调" |

### 4.2 强烈建议有的（影响体验）

| 素材 | 数量 | 说明 |
|---|---|---|
| 音效 | 6-8 个 | 发牌"唰"、翻牌"啪"、下注筹码碰撞、金瓜子获得（欢快上升音）、按钮点击、胜利小号、倒计时滴答 |
| 背景 | 1-2 张 | 木纹牌桌 / 暖色渐变 + 瓜子漂浮装饰 |
| 加载动画 | 1 个 | 仓鼠嗑瓜子 Lottie 或 SVG 逐帧 |

### 4.3 可选的（加分项）

| 素材 | 说明 |
|---|---|
| 可爱中文字体 | ⚠️ **注意授权**。站酷快乐体/思源黑体可商用；直接嵌网上下载的字体有侵权风险。**建议默认用系统字体，需要时再换** |
| 仓鼠表情包 | 等待中的情绪反馈（思考、得意、懊恼） |
| 二维码皮肤 | 把二维码模块点替换成小瓜子/仓鼠头，提升主题感 |

### 4.4 我不需要你提供的素材

SVG 头像、卡牌、瓜子、界面视觉、动画曲线，全部可以由我程序化生成并迭代，你只需审阅效果。**唯一需要外部输入的是：音效文件**（若你有音效库可直接给我文件路径；否则我用 Web Audio API 程序合成简单音效，质量一般但零依赖、零授权风险）。

---

## 5. 需要你提供什么

### 5.1 必需（阻塞上线）

| # | 项目 | 说明 |
|---|---|---|
| 1 | **腾讯云账号** | 需要你本人实名认证的账号。建议：你开通 CloudBase 环境，把我生成的代码包/镜像部署上去；或给我一个**仅部署权限**的子账号 SecretId/SecretKey<br>🔴 **不要把主账号密钥发给任何人**，包括我。若不愿提供密钥，你自己在控制台点"上传代码包部署"也行 |
| 2 | **CloudBase 环境 ID（envId）** | 创建环境后在控制台可见，前端 SDK 初始化和 CLI 部署都要用 |
| 3 | **一个域名** | 正式上线必需。需要你已购买或愿意购买（约 30-60 元/年） |
| 4 | **ICP 备案** | 🔴 **最长的阻塞项，7-20 天**。需要你的身份证/护照、域名、服务器/云资源信息、网站内容说明。**建议在开发的同时就提交备案**，否则代码写完了也上不了线 |
| 5 | **HTTPS 证书** | 腾讯云 SSL 可申请免费证书（3 个月有效期，需手动续）或用付费证书 |

### 5.2 强烈建议（决定要不要花钱）

| # | 项目 | 说明 |
|---|---|---|
| 6 | **套餐决策** | 免费体验版（无法绑域名，只能验证部署）→ 个人版 19.9 元/月（可上线）→ 标准版 199 元/月。前期建议个人版 |
| 7 | **"超限按量"开关** | 云托管必须开启，否则欠费即停服。你需要在控制台开启并设置告警 |
| 8 | **预算上限** | 出流量 0.8 元/GB 偏贵。若担心，可设月预算告警（如 50 元） |

### 5.3 可选

| # | 项目 | 说明 |
|---|---|---|
| 9 | 音效文件 | 见 §4.2 |
| 10 | 已有的仓鼠形象/品牌图 | 如果你有指定形象，给我参考图 |
| 11 | Git 仓库 | 若想用 Git Webhook 自动部署，需要 GitHub/Gitee/CNB 仓库（CloudBase 支持这四家 + 私有仓库） |

### 5.4 完全不需要你提供

- 数据库、Redis、对象存储配置（需求说不需要数据库，我按不需要设计）
- Nginx 配置（CloudBase 网关 + 静态托管已覆盖）
- Vercel 账号（§3.5 已建议降级）

---

## 6. 需要你决策的问题（影响架构，请先回答）

按重要性排序：

1. **金瓜子怎么存？**（§2.1）
   - A. 只存 localStorage，本机有效（推荐，最简单）
   - B. 接云数据库，跨设备永久累计（复杂度大幅上升，需账号体系）

2. **线上模式一个玩家输光后怎么办？**
   - A. 自动重置全员，所有人复活继续（需求字面意思）
   - B. 该玩家淘汰/观战，剩余玩家继续，直到剩一人

3. **短牌（德州扑克短牌）的具体规则？**
   - 36 张（去 2-5）、三同花 > 同花顺、A 可高可低 —— 确认是否按这个标准
   - 短牌用 ante 还是大小盲注？

4. **是否实现边池（Side Pot）？**
   - 多人全下必须做，否则筹码算错。做（推荐）还是限制"每人最多全下一次"来规避？

5. **PC 端访问怎么办？**
   - A. 显示"请用手机打开"引导页 + 二维码（推荐）
   - B. 完全不可用
   - C. 简单适配横屏

6. **线下计分模式的操作粒度？**
   - "左边收瓜子 / 右边出瓜子"：是操作自己的筹码，还是庄家/任意人可操作所有人？
   - 下注/弃牌在纯计分模式下具体做什么？

7. **部署目标优先级**
   - A. 先 CloudBase（按需求）
   - B. 先本地 Docker 跑通，CloudBase 后置（推荐，风险最低）

---

## 7. 建议的实施里程碑

| 阶段 | 内容 | 预估工作量 |
|---|---|---|
| **M0 契约冻结** | 回答 §6 的 7 个问题，定稿规则文档 | 0.5 天 |
| **M1 骨架** | 目录结构、`npm run dev` 一条命令、前后端连通、Socket.io echo | 0.5 天 |
| **M2 注册 + 大厅** | 昵称/头像注册、localStorage 自动登录、模式选择 | 1 天 |
| **M3 线下计分** | 房间创建/加入、玩家列表、加减瓜子、长按快捷、局数/盲注显示、结算 | 1.5 天 |
| **M4 线上长牌** | 洗牌发牌、四轮下注、全下+边池、牌型判定、摊牌结算、动画 | 2.5 天 |
| **M5 线上短牌** | 36 张牌堆 + 牌型表调整 | 0.5 天 |
| **M6 打磨** | 音效、金瓜子特效、断线重连恢复、空状态/异常提示 | 1 天 |
| **M7 部署** | Dockerfile、compose、CloudBase 静态托管 + 云托管、文档 | 1 天 |
| | **合计** | **≈ 8.5 个工作日** |

> 若 M4 的边池判定过于复杂，可先用"M3+M5+M6"交付线下模式和短牌框架，长牌作为二期。

---

## 8. 调研信息来源

关键结论均来自腾讯云官方文档（2026-09-20 抓取）：

- [云托管概述](https://docs.cloudbase.net/run/introduction) ｜ [WebSocket 使用](https://docs.cloudbase.net/run/develop/access/websocket) ｜ [使用限制](https://docs.cloudbase.net/run/limitation)
- [云托管部署方式](https://docs.cloudbase.net/run/deploy/deploy/introduce) ｜ [源代码部署](https://docs.cloudbase.net/run/deploy/deploy/deploying-source-code) ｜ [容器镜像部署](https://docs.cloudbase.net/run/deploy/deploy/deploying-image)
- [自定义域名](https://docs.cloudbase.net/run/deploy/networking/custom-domains) ｜ [环境变量](https://docs.cloudbase.net/run/deploy/configuring/environment/envs) ｜ [日志](https://docs.cloudbase.net/run/maintain/log)
- [静态网站托管概述](https://docs.cloudbase.net/hosting/introduce) ｜ [Vue SPA 部署教程](https://docs.cloudbase.net/recipes/add-hosting-vue) ｜ [控制台管理](https://docs.cloudbase.net/hosting/manage)
- [资源点价格文档（含套餐配额表）](https://cloud.tencent.com/document/product/876/127357)
- [云函数 WebSocket（2026-01-27 上线）](https://docs.cloudbase.net/cloud-function/develop/websocket)
- [HTTP 网关概述](https://docs.cloudbase.net/service/introduce) ｜ [自定义域名备案要求](https://docs.cloudbase.net/service/custom-domain)
- [备案云资源（云开发环境可备案）](https://cloud.tencent.com/document/product/243/18908)

**未能从一手来源确认的项（已在正文标注）**：
- CloudBase 云托管 + Socket.io 的官方示例或社区案例（官方 WS 示例用 `express-ws`）
- Vercel 在中国大陆的访问策略细节
- CloudBase 网关 WebSocket 空闲超时的精确可配置项（文档口径为 60s 硬上限）

---

## 9. 一句话总结

**能做，能自己部署在 CloudBase，但要先回答 §6 的 7 个问题（尤其是金瓜子存储方式），并在开发的同时启动 ICP 备案——那是唯一无法靠写代码压缩的时间成本。**
