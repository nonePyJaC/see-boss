# ============ dependencies ============
node_modules/
client/node_modules/

# ============ build output ============
client/dist/
dist/
*.tsbuildinfo

# ============ env (contains secrets, never commit) ============
.env
.env.local
.env.*.local
client/.env

# ============ logs ============
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============ os / editor ============
.DS_Store
Thumbs.db
.vscode/
.idea/

# ============ test & temp artifacts ============
test-out.txt
dist.zip
*.tmp

# ============ SQLite data (accounts / golden seeds / ledger) ============
# Real user data lives here. Never commit it.
server/data/
*.db
*.db-wal
*.db-shm

# ============ pm2 runtime ============
.pm2/
logs/

# ============ deploy leftovers ============
cloudbaserc.local.json

# ============ local images ============
*.png
