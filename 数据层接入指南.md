# 数据层接入指南

> 状态：**代码已就绪，等待 4 项实测**
> 相关文件：[db/schema.sql](../db/schema.sql) ｜ [client/src/data/](../client/src/data/) ｜ [cloud-functions/signatures.md](../cloud-functions/signatures.md)

---

## 1. 已完成的代码

### 架构
```
前端 (Vue)
  └── data/repo.js          ← 唯一入口，按 VITE_DATA_SOURCE 切换
        ├── data/local-repo.js     localStorage 实现（当前默认）
        └── data/cloud-repo.js     CloudBase PG 实现
              └── data/cloud-repo-internal.js   app 单例 + 匿名登录
  └── data/realtime.js       ← 实时同步：cdc / broadcast / poll 三选一

数据库 (PostgreSQL)
  ├── users / seed_ledger     用户档案 + 金瓜子账本
  ├── rooms / room_members    房间 + 座位
  ├── hands                   底牌（RLS 限本人）
  ├── decks                   牌堆（前端零权限）
  └── history                 线下对局历史

云函数 (待部署)
  ├── hand-create / hand-act / hand-showdown   牌局逻辑
  ├── room-join / room-leave                   房间管理
  └── offline-settle / offline-pot             线下结算
```

### 关键设计
| 决策 | 原因 |
|---|---|
| 前端只读，写全走云函数/RPC | 防作弊：玩家不能直接改自己筹码 |
| `decks` 表不授予任何 GRANT | 前端连读的权限都没有，看不到牌堆 |
| RLS 用 `(select auth.uid())` 包裹 | 否则 PG 对每一行都调用函数，性能差数个数量级 |
| 线上房间 `leave_online_room` RPC 直接删 | 零留存，规避法律风险 |
| `transfer_seeds` 用 SECURITY DEFINER + 函数内校验 | PostgREST 网关**不检查** `GRANT EXECUTE`，任何角色都能调 RPC |

---

## 2. 需要你做的 4 项实测

按顺序做，每项结果决定后续架构。

### ① 确认环境地域 ⚠️ 最先做

控制台「概览」页看环境地域。

| 地域 | PG CPU 单价 | 3000 点可支撑 |
|---|---|---|
| 上海 | 342 点/核·时 | ~17.5 小时/月 |
| 新加坡 | 587 点/核·时 | ~10.2 小时/月 |

**上海便宜 1.7 倍，且新加坡地域不支持云托管。**

---

### ② 执行 schema.sql

控制台「SQL 型数据库 → PostgreSQL 管理 → 数据编辑器」，把 [db/schema.sql](../db/schema.sql) 的内容**逐段**执行。

注意：
- `ExecutePGSql` 每次只能执行一条 SQL，不能分号拼接多条
- 部分 DDL 可能报 `InternalError`，用 `DO $$ ... $$` 块包装重试
- 建议按文件的注释分块执行（0-17 段）

执行完验证：
```sql
-- 应返回 6 张表
select tablename from pg_tables where schemaname='public' order by tablename;

-- 应返回所有策略
select tablename, policyname from pg_policies where schemaname='public';
```

---

### ③ 开启匿名登录

控制台「身份认证 → 登录方式」→ 开启「**匿名登录**」。

⚠️ **这一步是必须的**：只用 Publishable Key 而不做 `signInAnonymously()`，所有玩家会共享 `sub="anon"`，RLS 无法按用户隔离数据。

---

### ④ 测试前端能否直连（最关键）

这是架构分岔点。先确认密钥：

控制台「设置 → API Key」→ 生成 **Publishable Key**（只能生成一次，妥善保存）。

```bash
# D:\newPoker\
copy .env.example .env
```

编辑 `.env`：
```
VITE_DATA_SOURCE=cloud
VITE_CLOUDBASE_ENV_ID=see-boss-d2gjggfbz8808d1d4
VITE_CLOUDBASE_PUBLISHABLE_KEY=<你生成的 key>
VITE_REALTIME_MODE=poll
```

```bash
npm run dev
```

浏览器打开 `http://localhost:5173`，F12 看 Console：
- ✅ 无 CORS 错误、无 401 → 前端直连 PG 可行，架构成立
- ❌ CORS 错误 → 去「设置 → 安全来源」加 `localhost:5173`
- ❌ 401 / permission denied → RLS 或匿名登录问题，把报错发我

---

## 3. 接下来要测的（依赖 ④ 的结果）

### 若前端能直连（预期路径）

**A. 测实时同步**

按优先级试三种模式，改 `.env` 的 `VITE_REALTIME_MODE`：

1. `poll`（轮询）— 必然可用，先用它跑通
2. `broadcast` — 需确认云函数能否从服务端发广播
3. `cdc` — 官方文档自相矛盾（迁移页说 PG 模式不支持，但 9 月新文档有完整 API）

**B. 测云函数能否连 PostgreSQL**

免费版云函数**不支持 VPC**，而文档说云函数「优先使用内网访问」。部署一个测试函数：

```js
// 云函数 index.js
const { Pool } = require('pg')
exports.main = async () => {
  const pool = new Pool({
    host: process.env.PGHOST,
    port: Number(process.env.PGPORT || 5432),
    database: process.env.PGDATABASE,
    user: process.env.PGUSER,
    password: process.env.PGPASSWORD,
    ssl: { rejectUnauthorized: false },
    max: 3,
  })
  const r = await pool.query('select 1 as ok')
  return r.rows
}
```

在云函数环境变量里配 `PGHOST` / `PGPORT` / `PGDATABASE` / `PGUSER` / `PGPASSWORD`（控制台「连接信息」里能看到）。

- ✅ 返回 `[{ok:1}]` → 云函数能连，按 [signatures.md](../cloud-functions/signatures.md) 部署全部函数
- ❌ 连不上 → **逻辑前移到 PG 的 SECURITY DEFINER 函数**（`transfer_seeds` 已经是这个模式，可以照写），用 `FOR UPDATE` 锁行保证并发

**C. 关注额度**

免费版 3000 点/月，云函数 3s 超时。跑几局后去「套餐用量页」看实际消耗，重点观察：
- 数据库调用次数（200 点/万次）
- PostgreSQL CPU（342 点/核·时，按 5 分钟窗口判活）
- 云函数资源使用量

---

## 4. 需要你提供的东西

| # | 项目 | 用途 |
|---|---|---|
| 1 | **Publishable Key** | 前端直连 PG 的凭证（可暴露，role=anon） |
| 2 | **PG 连接信息** | 云函数连数据库用（host/port/db/user/password） |
| 3 | **环境地域** | 决定成本预估 |
| 4 | 4 项实测结果 | 决定最终架构 |

🔴 **密钥安全**：
- Publishable Key 可以进 `.env`（它设计上就可暴露）
- **PG 密码只在云函数环境变量里配，不要发给任何人，也不要写进代码**
- 绝不要用 API Key（service_role 有 BYPASSRLS）

---

## 5. 我已预留的接入点

| 位置 | 内容 |
|---|---|
| [db/schema.sql](../db/schema.sql) | 6 张表 + 视图 + 触发器 + GRANT + RLS + 5 个 RPC，可直接执行 |
| [cloud-functions/signatures.md](../cloud-functions/signatures.md) | 7 个云函数的请求/响应签名 |
| [client/src/data/repo.js](../client/src/data/repo.js) | 统一入口，`VITE_DATA_SOURCE=cloud` 一键切换 |
| [client/src/data/realtime.js](../client/src/data/realtime.js) | 三种实时策略可切换，CDC 失败自动降级轮询 |
| [.env.example](../.env.example) | 环境变量模板 |

**前端业务代码（OfflineRoomView / OnlineRoomView / ProfileView / HistoryView）暂未切换到 repo** —— 等 ④ 有结果再改，避免返工。

---

## 6. 一句话

**代码都写好了，就差你提供 Publishable Key + PG 连接信息 + 跑一遍 4 项实测。最关键的是第 ④ 步：确认前端能否直连 PG，这决定整个架构走向。**
